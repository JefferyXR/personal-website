# Requirements Document

## Introduction

This feature refreshes the typography and footer contact legibility of the personal engineering portfolio site (a static HTML5 UP–derived site deployed to GitHub Pages). Two outcomes were in scope for the first change set:

1. The footer email link becomes visibly darker and meets a measurable contrast target on all nine content pages.
2. All heading and subheading text switches to a geometric display typeface, and all descriptive/body text switches to a modern grotesque typeface, each with defined sizes and weights, self-hosted webfonts, and defined fallback behaviour.

### Change Set 2 (follow-up)

**Change Set 1 is implemented and merged to `main`.** The owner has since requested six follow-up changes, recorded here as amendments to existing requirements and as Requirements 10 through 14. Requirement numbering 1–9 is preserved; nothing is renumbered.

| # | Change | Where recorded |
|---|---|---|
| 1 | Footer email link colour moves from `#4a5158` (7.38:1) to `#3a4148` (9.49:1) | Requirement 1 c7, c12 amended; Requirement 8 c7 amended |
| 2 | Project card titles render horizontally centred on the homepage | **Requirement 10** (new) |
| 3 | Top nav links, Read More / View Model buttons, and skills pills move to Telegraf Ultrabold (800) | **Requirement 11** (new); Requirement 5 c2 amended |
| 4 | Skills pill box geometry corrected for the heavier label text | **Requirement 12** (new, depends on Requirement 11) |
| 5 | Footer "Fonts & icons" line becomes a Back to top control, with the HTML5 UP credit retained and reworded | **Requirement 13** (new); Requirement 8 c5 amended |
| 6 | `#copyright` contrast raised to at least 4.5:1 — **resolves conflict C3** | **Requirement 14** (new); Requirement 1 c11 amended |
| 7 | Stored-licence-text obligation no longer applies to Horizon | Requirement 9 c2 amended; c9–c11 added |

### Change Set 3 (follow-up, this amendment)

**Change Set 2 is implemented on branch `spec/typography-refresh-change-set-2` (PR #2), together with a follow-up fix that removed the global `scroll-behavior` declaration.** The owner has since requested three more changes, recorded here as amendments to existing requirements and as Requirements 15 through 17. Requirement numbering 1–14 is preserved; nothing is renumbered.

| # | Change | Where recorded |
|---|---|---|
| 1 | The Copyright_Block divider bar sits at the exact horizontal centre of the block, independent of the two label widths | **Requirement 15** (new) |
| 2 | The Nav_Panel_Toggle and the Nav_Panel_Link elements move to Telegraf Ultrabold (800) | **Requirement 16** (new); Requirement 11 c15 amended; the **Chrome_Text** and **Bold_Chrome_Text** glossary entries amended |
| 3 | `README.md` returns to a short form; the stylesheet regeneration procedure moves to `docs/stylesheet-sync.md`, linked from the README in one line | **Requirement 17** (new); Requirement 7 c5 amended and c11–c13 added; Requirement 9 c3 amended; Requirement 4 c4 amended |

Change Set 3 adds no font file, changes no `@font-face` rule, changes no palette value, and changes no markup inside any of the nine Content_Pages. Changes 1 and 2 are stylesheet-only; change 3 touches `README.md`, the new `docs/stylesheet-sync.md`, and the prune step of `.github/workflows/static.yml`.

### Change Set 4 (follow-up, this amendment)

**Change Set 3 is implemented.** The owner has since **deleted the `assets/sass/` tree — 31 files.** This amendment records the consequences. It adds no new requirement and renumbers nothing outside Requirement 7; **Requirement 7 is rewritten, retitled, and its criteria are renumbered**, because its whole subject — two artifacts that had to agree — no longer exists.

| # | Change | Where recorded |
|---|---|---|
| 1 | `assets/sass/**` deleted. `assets/css/main.css` and `assets/css/noscript.css` are the only stylesheet artifacts, and `main.css` is a **source** file rather than a compiled output | **Requirement 7 rewritten and retitled**; the `Stylesheet_Source` glossary entry removed and `Compiled_Stylesheet` / `Sync_Document` redefined; criteria swept in Requirements 1, 3, 4, 5, 8, 10, 11, 12, 15, 16 and 17; Assumptions item 16 |
| 2 | The fully-overridden `body.home #main .button.skills, body.home #main .actions .button` rule deleted from `main.css` | Requirement 12, note under the preamble |
| 3 | The `assets/js/waterParticles.js` pointer-tracking bug fixed — the listener moves to `window` and `mouse.active` is set inside the handler, with release listeners | Requirement 8 criterion 6, whose wording is unchanged; the fix is what makes it hold |
| 4 | Two dangling `background-image` layers (`images/overlay.png`, `images/bg.jpg` — neither file exists) removed from both stylesheets, together with a broken `@import url(font-awesome.min.css)` in `noscript.css` | Requirement 7 criterion 2, note |
| 5 | `docs/stylesheet-sync.md` rewritten as a **five-item maintenance note for `main.css` alone**, with no regeneration or parity procedure in it | Requirement 7 criteria 6–9 and Requirement 17 criteria 8, 9 amended |

Change Set 4 adds no font file, changes no `@font-face` rule, changes no colour value, and changes no markup on any of the nine Content_Pages.

The chosen typefaces are the two the site owner originally named:

- **Horizon** by **Alberto Fontense** (also spelled Fontese) for Heading_Text — a bold, extra-wide geometric sans serif with round circular edges and tight apertures. The designer distributes it free for personal use; a commercial licence is required for client work, commercial branding, or any monetised use. It is distributed in OTF and WOFF2, and WOFF2 is present in the free demo. Character support covers Basic Latin, Latin-1 and Latin Extended. The designer's own channels appear to be VP Creative Shop and [Creative Market](https://edocs.creativemarket.com/fontense/2189003-Horizon-Wide-Sans-Serif); the aggregator mirror [fontdownloader.net](https://fontdownloader.net/horizon-font/) was consulted for the specification above but is not an approved download source (see Requirement 9).
- **Telegraf** by **Pangram Pangram Foundry** for Body_Text — a neo-grotesque with technical detailing that stays readable at text sizes. The [foundry's product page](https://pangrampangram.com/products/telegraf/) offers a free-to-try tier, and the [foundry FAQ](https://pangrampangram.com/pages/faq) describes that tier as covering personal, non-commercial use and names personal portfolios — in PDF, print, or on the web — among the permitted uses. Free downloads carry the complete glyph set but only a few selected styles and weights; WOFF and WOFF2 ship only with a paid licence. The foundry's terms restrict customising or modifying the font file itself. Paid Web licences are scoped per domain and priced by monthly pageviews.

Both licences are usable here **only because this Site is strictly a personal, non-commercial showcase built to support job applications**. No purchase is therefore required. That non-commercial status is not an incidental fact about the Site — it is a standing constraint on what the Site may ever contain. Requirement 9 makes the constraint explicit and testable: if the Site later advertises paid services, freelance availability, rates, or any other monetisation, both licences cease to apply and paid licences must be bought before deployment.

Before Change Set 1 the site loaded Merriweather and Source Sans Pro through a Google Fonts `@import` at the top of `assets/css/main.css`, and every typeface value was expressed twice — once as a variable-map lookup in a parallel `assets/sass/` tree and once as a resolved literal in `main.css` — with a small number of hardcoded overrides such as the project card `h2` rule. The repository has never contained build tooling: nothing compiled that tree, and `main.css` was hand-edited beside it. **Change Set 4 deletes the tree** (Assumptions item 16), so `assets/css/main.css` and `assets/css/noscript.css` are the only stylesheet artifacts and `main.css` is the stylesheet source.

*Licence terms above are summarised, not quoted; verbatim-quotation limits were respected and font licence details were rephrased for compliance with licensing restrictions. The linked sources are authoritative for the actual terms.*

## Glossary

- **Site**: The static portfolio site rooted at the repository root, deployed to GitHub Pages by `.github/workflows/static.yml`.
- **Content_Page**: Any of the nine deployed HTML pages: `index.html`, `arduino.html`, `cad.html`, `calculator.html`, `church.html`, `fluid_sim.html`, `killerbyte.html`, `launchtoy.html`, `vexlego.html`.
- **Compiled_Stylesheet**: The browser-loaded CSS file `assets/css/main.css`, which every Content_Page links directly. **Amended in Change Set 4:** this is the stylesheet **source**, not an output. The `assets/sass/` tree it once mirrored has been deleted, nothing generates `main.css`, and it is edited directly. The name is retained because every requirement below already refers to it; read "compiled" as historical, not as a description of how the file is produced.
- **Noscript_Stylesheet**: The CSS file `assets/css/noscript.css`, loaded by every Content_Page inside a `<noscript>` element. Named in Change Set 4 because two of that change set's edits reach it.
- **Heading_Font**: The Horizon typeface by Alberto Fontense, applied to Heading_Text.
- **Body_Font**: The Telegraf typeface by Pangram Pangram Foundry, applied to Body_Text and Chrome_Text.
- **Heading_Text**: Text rendered by the `h1` through `h6` elements on any Content_Page, including the intro `h1`, the project card `h2` elements, and the footer `h3` labels.
- **Body_Text**: Text rendered by paragraph, list, `input`, `select`, and `textarea` elements, plus any element inheriting the body `font-family` declared on `body` in the Compiled_Stylesheet, including project card description paragraphs.
- **Chrome_Text**: Small interface text with a declared `font-size` at or below 0.9rem that, before Change Set 1, inherited the heading family. Seven groups, named by the Compiled_Stylesheet selector that governs each: skills and Read More button labels (`.button`), form labels (`#footer form label` and the base `label` rule), pagination links (`.pagination a`), table headers (`table th`), the Nav_Panel_Link elements (`#navPanel .links li a`) and the Nav_Panel_Toggle (`#navPanelToggle`), and the Copyright_Block (`#copyright`). **Amended in Change Set 4** to name selectors rather than the `assets/sass/**` files that used to carry them; the membership of the group is unchanged. **Amended in Change Set 3** in two respects, both corrections of the original wording rather than extensions of scope: the size bound reads "at or below 0.9rem" instead of "below 0.9rem", and the Nav_Panel_Toggle is named. The Nav_Panel_Link and Nav_Panel_Toggle declarations are each exactly 0.9rem, so the original strict inequality excluded by accident the navigation panel elements that the same entry already listed by name.
- **Card_Heading**: The project card `h2` element, selected by `body.home #main > .posts > article h2` in the Compiled_Stylesheet. Before Change Set 1 it carried a hardcoded `font-family: Merriweather, Georgia, serif` declaration; it now declares the Heading_Font stack and retains its own `font-size: 1.1rem` and `text-transform: none`.
- **Card_Header_Band**: The `header` element wrapping a Card_Heading on the homepage, selected by `body.home #main > .posts > article header` in the Compiled_Stylesheet, which declares `background-color: #12263a`, `padding: 0.85rem 1rem`, and — since Change Set 2 — `text-align: center`. It contains the Card_Heading and no other content.
- **Bold_Chrome_Text**: The groups of Chrome_Text that render at the bold weight rather than the body weight. Requirement 11 established three: the top navigation links (`#nav ul.links` and their `a` descendants), every `.button` label including Read More and the `.button.primary` / `.button.primary.small.fit` View Model variants, and the skills pill labels (`body.home #main .button.skills` and the grouped `.button.skills` rule elsewhere). **Change Set 3 adds two more**: the Nav_Panel_Toggle (`#navPanelToggle`) and the Nav_Panel_Link elements (`#navPanel .links li a`). All five are governed by rules in the Compiled_Stylesheet. Requirement 16 governs the two added sites; Requirement 11 continues to govern the original three. Bold_Chrome_Text remains a subset of Chrome_Text and is governed by Requirement 5 except where Requirement 11 or Requirement 16 states otherwise.
- **Nav_Panel_Toggle**: The `<a href="#navPanel" id="navPanelToggle">Menu</a>` element injected into `#wrapper` by `assets/js/main.js` and styled by the `#navPanelToggle` rule in the Compiled_Stylesheet. That rule declares the Body_Font stack, `font-size: 0.9rem`, a `font-weight` that Requirement 16 moves to 800, `letter-spacing: 0.05em`, `text-transform: uppercase`, `position: fixed` at `top: 0.75rem` and `right: 0.75rem`, and `padding: 0.375rem 1.25rem`, reducing to `font-size: 0.8rem` and `padding: 0.25rem 1rem` at the `<=small` breakpoint. It renders `display: block` at the `<=medium` breakpoint and `display: none` above it. Its `:before` pseudo-element renders the `\f0c9` Font Awesome glyph with `margin-right: 0.5rem`, and its `.alt` variant applies a background, box shadow, and darker colour once the visitor has scrolled past the header.
- **Nav_Panel_Link**: Any `a` element matched by `#navPanel .links li a` in the Compiled_Stylesheet, whose rule declares the Body_Font stack, `font-size: 0.9rem` **twice** (a harmless pre-existing duplicate, retained deliberately — see Requirement 7 criterion 8 and Assumptions item 14), a `font-weight` that Requirement 16 moves to 800, `letter-spacing: 0.05em`, `display: block`, `padding: 0.75rem 0`, and `text-transform: uppercase`. The Nav_Panel_Link elements are **the same DOM nodes** as the top navigation links: `assets/js/main.js` appends the children of `#nav` into `#navPanel > nav` at the `<=medium` breakpoint and returns them to `#nav` above it, so "Projects" and "CAD Gallery" are one pair of anchors that changes parent, not two independent pairs.
- **Copyright_Row**: The `ul` element inside the Copyright_Block, selected by `#copyright ul` in the Compiled_Stylesheet, declaring `list-style: none`, `margin: 0`, and `padding-left: 0`. It contains exactly the two Copyright_Item elements and no other content.
- **Copyright_Item**: Either of the two `li` elements inside the Copyright_Row. The first holds the Back_To_Top_Control, whose visible label is "Back to top"; the second holds the literal text "Design:" followed by the Design_Credit link "HTML5 UP". In the Side_By_Side_Layout each item declares `display: inline-block`, `line-height: 1`, `margin-left: 1rem`, and `padding-left: 1rem`, with the first item resetting its left border, margin, and padding to zero. In the Stacked_Layout each item declares `display: block`, `border-left: 0`, `margin: 1rem 0 0 0`, and `padding-left: 0`, with the first item resetting its top margin to zero.
- **Copyright_Divider**: The vertical rule between the two Copyright_Item elements, rendered by the `border-left: solid 2px` declaration on the second Copyright_Item. Its colour comes from the omitted colour component of that `border-left` shorthand, which resolves to `currentColor` and therefore to the Copyright_Block `color` value that Requirement 14 sets. The Copyright_Divider exists only in the Side_By_Side_Layout; the Stacked_Layout removes it through `border-left: 0`.
- **Side_By_Side_Layout**: The Copyright_Row layout that applies while the viewport width is wider than the `<=xsmall` breakpoint — that is, at 481px and above, given the `max-width: 480px` media block in the Compiled_Stylesheet — in which both Copyright_Item elements are `display: inline-block` on one line and the Copyright_Divider is rendered.
- **Stacked_Layout**: The Copyright_Row layout that applies at the `<=xsmall` breakpoint — 480px and below — in which each Copyright_Item is a full-width `display: block` box on its own line and no Copyright_Divider is rendered.
- **Sync_Document**: The separate repository document at `docs/stylesheet-sync.md` that `README.md` links in one line under Requirement 17. **Redefined in Change Set 4:** it is a **maintenance note for the Compiled_Stylesheet**, holding the five non-obvious facts about `main.css` that Requirement 7 criteria 6 through 9 pin, plus the standing licence conditions. It no longer holds a regeneration or parity procedure, because there is no second artifact to regenerate from or compare against. The filename is kept as it is; renaming it would break the README link and the Provenance_Record cross-reference for no gain.
- **Readme_Credits**: The `## Credits` section of `README.md`, which carries the font attributions required by Requirement 9 criterion 3 and the HTML5 UP Creative Commons Attribution 3.0 template attribution recorded in Requirement 13.
- **Skills_Pill**: An `a.button.skills` element. Two geometries were declared in the Compiled_Stylesheet: the **homepage geometry** (`body.home #main .button.skills`, the only one with rendered instances) and the **wider-context geometry**, whose declaration site — the grouped `body.home #main .button.skills, body.home #main .actions .button` rule — **Change Set 4 deleted** as dead. See the note under Requirement 12.
- **Copyright_Block**: The `div#copyright` element present inside the footer on all nine Content_Pages. Its text colour was originally declared as white at alpha 0.25 over the background `#1e252d`; Requirement 14 raises that alpha.
- **Back_To_Top_Control**: The interactive control inside the Copyright_Block, introduced by Requirement 13, that returns the visitor to the top of the current document.
- **Design_Credit**: The visible attribution inside the Copyright_Block that credits HTML5 UP for the template design and links to `https://html5up.net`. It replaces the pre-change "Fonts & icons: HTML5 UP" wording, which is no longer accurate because the Webfont_Bundle fonts are Horizon and Telegraf.
- **Provenance_Record**: The per-file font provenance record required by Requirement 9, stored at `assets/webfonts/FONT-PROVENANCE.md`.
- **Footer_Email_Link**: The `mailto:` anchor inside the footer contact `<section>` labelled by `<h3>Email</h3>` on each Content_Page.
- **Footer_Background**: The footer surface colour, currently `#f5f5f5` (the `alt` palette `bg` value).
- **Webfont_Bundle**: The self-hosted font files for Heading_Font and Body_Font stored under `assets/webfonts/`, together with their `@font-face` declarations.
- **Vendor_Supplied_Format**: A font file format obtained directly from the designer or foundry for the licence tier in use, without local conversion, subsetting, or any other modification of the file.
- **Fallback_Stack**: The ordered list of substitute font families declared after Heading_Font or Body_Font in a `font-family` declaration.
- **Contrast_Ratio**: The WCAG 2.1 relative luminance contrast ratio between a foreground colour and its background colour.
- **Monospace_Text**: Text rendered by `code` and `pre` elements, governed by the `'Courier New', monospace` stack the Compiled_Stylesheet declares for them.
- **Non_Commercial_Use**: Use of the Site solely as a personal showcase supporting job applications, with no advertisement of paid services, freelance availability, rates, sponsorship, or any other monetisation.

## Requirements

### Requirement 1: Darker Footer Email Link

**User Story:** As a visitor reading the footer, I want the email address to be clearly legible against the footer background, so that I can read and use the contact address without straining.

The Footer_Email_Link originally rendered at `#717981` on Footer_Background `#f5f5f5`, a Contrast_Ratio of 4.05:1 at a font size of 0.8rem, which falls below the WCAG AA threshold of 4.5:1 for text of that size. Change Set 1 shipped `#4a5158` (7.38:1 measured). **Change Set 2 replaces that with `#3a4148`, which measures 9.49:1 against Footer_Background** — the darker of the two candidates originally considered. The 9.49:1 figure is the computed value and supersedes the 9.5:1 figure cited in earlier drafts. See Assumptions and Open Questions item 7, which records the decision.

The `>= 7.0:1` threshold in criterion 1 is **unchanged** by this amendment: `#3a4148` clears it with a wider margin than `#4a5158` did. Only criteria 7, 11, and 12 change, plus the `alt.fg-link` token value and Requirement 8 criterion 7.

#### Acceptance Criteria

1. WHILE the Footer_Email_Link is in its default state (not hovered, not focused, not active), THE Site SHALL render its text at a colour whose Contrast_Ratio against Footer_Background `#f5f5f5` is at least 7.0:1.
2. THE Site SHALL render the Footer_Email_Link at a colour with a lower relative luminance than `#717981`.
3. THE Site SHALL apply one identical Footer_Email_Link default-state colour value on all nine Content_Pages.
4. WHEN a visitor moves the pointer onto the Footer_Email_Link, THE Site SHALL apply the existing accent colour `#18bfef` as the text colour within 200 ms of the pointer entering the link area.
5. WHEN a visitor moves the pointer off the Footer_Email_Link, THE Site SHALL restore the default-state colour defined by criterion 1 within 200 ms.
6. WHILE the Footer_Email_Link holds keyboard focus, THE Site SHALL render a focus indicator that is at least 2 CSS pixels thick, spans at least the full width of the link text, and has a Contrast_Ratio of at least 3.0:1 against Footer_Background, and SHALL retain that indicator while the pointer is simultaneously hovering the link.
7. THE Site SHALL render the Footer_Email_Link underline in the default, hover, and focused states at a Contrast_Ratio against Footer_Background of at least 3.0:1, and SHALL declare the default-state underline as a fully opaque colour equal to the default-state text colour value declared by criterion 12, so that the underline tracks the text colour rather than compositing an alpha value against Footer_Background.
8. THE Site SHALL render the Footer_Email_Link at a declared `font-size` of at least 0.8rem on all nine Content_Pages.
9. THE Site SHALL preserve the `mailto:jefferyxross@gmail.com` destination and the visible email address text of the Footer_Email_Link on all nine Content_Pages.
10. THE Site SHALL restrict the darker colour change to the Footer_Email_Link only.
11. THE Site SHALL leave the declared colour values of the `<h3>Email</h3>` label and the footer social icon links identical to their pre-change values on all nine Content_Pages. **This criterion no longer applies to the Copyright_Block**, which Requirement 14 requires to be recoloured; the exemption is limited to the Copyright_Block and changes nothing about the `<h3>Email</h3>` label or the footer social icon links, both of which remain bound by this criterion. The exemption exists because the Copyright_Block now hosts an interactive control (the Back_To_Top_Control) rather than static text only, and the owner has reversed the earlier "email link only" ruling for that element specifically.
12. THE Site SHALL declare the Footer_Email_Link default-state colour as the literal value `#3a4148`, which measures a Contrast_Ratio of 9.49:1 against Footer_Background `#f5f5f5` and a lower relative luminance than `#717981`, and therefore satisfies criteria 1 and 2.
13. THE Site SHALL declare the value required by criterion 12 at every Footer_Email_Link colour and underline-colour declaration in the Compiled_Stylesheet, and SHALL leave zero occurrences of the superseded literal `#4a5158` in the Compiled_Stylesheet as a Footer_Email_Link colour or underline colour, including inside comments. **Amended in Change Set 4:** the clause naming the `alt` palette `fg-link` token in `assets/sass/libs/_vars.scss` is removed with the file. One artifact now carries the value, so "declare it in the token and mirror it" becomes "declare it at every site", and the zero-occurrence rule has one place left to hold rather than two.

### Requirement 2: Self-Hosted Webfont Provisioning

**User Story:** As the site owner, I want Horizon and Telegraf served from the repository, so that pages render in the intended typefaces without depending on a third-party font service.

Horizon is distributed with a WOFF2 face in its free tier, so Heading_Font is served in WOFF2. Telegraf's free tier ships desktop formats (OTF/TTF) only — WOFF2 requires a paid Web licence, and converting the supplied file locally may breach the foundry's restriction on modifying the font file. The delivery decision is now settled: Body_Font is served as the vendor-supplied OTF or TTF file exactly as the foundry supplies it, with no conversion and no purchase, and the larger transfer size is accepted under a 600 KB Webfont_Bundle budget. See Assumptions and Open Questions item 1, which records that decision. Because OTF and TTF are not pre-compressed the way WOFF2 is, the per-file bound in criterion 13 and the minimum-face-count rule in criterion 16 exist to keep the payload in check.

#### Acceptance Criteria

1. THE Site SHALL store the Webfont_Bundle files for Heading_Font and Body_Font under `assets/webfonts/`, alongside the existing Font Awesome webfont files.
2. THE Site SHALL provide each Heading_Font face in WOFF2 format, and SHALL reference no other font format for Heading_Font.
3. THE Site SHALL provide each Body_Font face as the unconverted OTF or TTF file supplied by Pangram Pangram Foundry under its free tier, that file being the Vendor_Supplied_Format for the licence tier in use, and SHALL reference no Body_Font file in any other format and no Body_Font file produced by local format conversion.
4. THE Webfont_Bundle SHALL include exactly one Heading_Font face, at the single `weight-heading` value declared for Heading_Text.
5. THE Webfont_Bundle SHALL include exactly one Body_Font face per weight declared for Body_Text in Requirement 4, and each included face SHALL be a style present in the licensed Telegraf download.
6. THE Compiled_Stylesheet SHALL declare exactly one `@font-face` rule per Heading_Font and Body_Font file in the Webfont_Bundle, and each rule SHALL declare a `font-family` name identical to the first family named in the corresponding `family-heading` or `family` stack, a `font-weight` equal to the weight of the referenced file, and a `format()` hint matching the referenced file's actual format: `opentype` for an OTF Body_Font file, `truetype` for a TTF Body_Font file, and `woff2` for the Heading_Font file.
7. THE Compiled_Stylesheet SHALL reference each webfont file using a path relative to `assets/css/main.css`, containing no absolute path and no URL scheme or host.
8. THE Compiled_Stylesheet SHALL declare `font-display: swap` in every `@font-face` rule for Heading_Font and Body_Font.
9. THE Compiled_Stylesheet SHALL declare a `unicode-range` covering at minimum the Basic Latin and Latin-1 Supplement character ranges for each `@font-face` rule that references a subset font file.
10. WHEN the GitHub Pages workflow in `.github/workflows/static.yml` deploys the repository, THE Site SHALL serve every Webfont_Bundle file referenced by the `@font-face` rules from the same origin as the requesting Content_Page, with each request returning a successful response.
11. THE Site SHALL remove the Google Fonts `@import` declaration for Merriweather and Source Sans Pro from the Compiled_Stylesheet, and SHALL declare no `@font-face` rule and no `font-family` value naming Merriweather or Source Sans Pro.
12. THE Site SHALL keep the sum of the stored file sizes of the Heading_Font and Body_Font files in the Webfont_Bundle at or below 600 KB, excluding the existing Font Awesome webfont files, because uncompressed desktop formats transfer substantially more bytes than WOFF2.
13. THE Site SHALL keep each individual Body_Font file in the Webfont_Bundle at or below 400 KB stored, because OTF and TTF files are not pre-compressed the way WOFF2 files are and each face is therefore paid for in full at transfer time.
14. THE Compiled_Stylesheet and every Content_Page SHALL contain no reference to a font resource hosted on any origin other than the deployed Site origin.
15. IF a request for a Webfont_Bundle file referenced by an `@font-face` rule returns an unsuccessful response, THEN THE Site SHALL continue to render all affected text using the declared Fallback_Stack, with no text hidden and no change to declared sizes, weights, or colours.
16. THE Webfont_Bundle SHALL include no Body_Font face beyond those required to satisfy the `weight` and `weight-bold` values declared for Body_Text in Requirement 4, because each additional uncompressed face adds its full stored size to the transfer cost.
17. WHEN the design phase fixes the `@font-face` rules for Body_Font, THE Site SHALL record in the design document, for each Body_Font file requested from the deployed GitHub Pages origin, the `Content-Encoding` response header value and the measured over-the-wire transfer size in bytes, so that the real transfer cost of the OTF or TTF delivery is established rather than assumed.
18. THE Site SHALL keep the measured over-the-wire transfer size of each Body_Font file at or below its stored file size as bounded by criterion 13.

### Requirement 3: Display Typeface for Headings

**User Story:** As a visitor, I want headings rendered in a distinctive geometric display typeface, so that the page hierarchy reads clearly and conveys a technical identity.

Horizon ships one solid text face. The remaining styles in the family — Outline, Outline Two, Lines, Lines Two — are decorative variants, not additional weights, so the heading hierarchy is carried by size alone. Horizon is also extra-wide, which drives the letter-spacing and `h1` sizing criteria below.

#### Acceptance Criteria

1. THE Compiled_Stylesheet SHALL declare, for every rule that sets a `font-family` for Heading_Text, a stack whose first family is Heading_Font.
2. THE Site SHALL resolve the computed `font-family` of every `h1`, `h2`, `h3`, `h4`, `h5`, and `h6` element on all nine Content_Pages to a stack whose first family is Heading_Font.
3. THE Site SHALL render the Card_Heading in Heading_Font by replacing the hardcoded `Merriweather, Georgia, serif` declaration on that rule, and SHALL leave no remaining `font-family` declaration in the Compiled_Stylesheet that applies a family other than Heading_Font to Heading_Text.
4. THE Compiled_Stylesheet SHALL declare one heading `font-weight` value, equal to the weight of the single solid Heading_Font face present in the Webfont_Bundle, and SHALL declare that same value at every heading level so that no browser-synthesized bold is applied to Heading_Text. Heading_Font is inherently heavy, so no visual weight is lost by declaring a single weight.
5. THE Site SHALL declare an explicit `font-size` for each heading level giving a strictly decreasing scale in which each level is at least 0.1rem larger than the level below it, with `h2` at 1.75rem, `h3` at 1.25rem, `h4` at 1rem, `h5` at 0.9rem, and `h6` at 0.8rem.
6. THE Site SHALL declare the intro `h1` `font-size` at the largest value at or below 4rem that satisfies criteria 10, 11, and 12, reducing the value from 4rem as far as Heading_Font's advance widths require.
7. THE Site SHALL declare a `letter-spacing` value for uppercase Heading_Text of at least -0.02em and at most 0.02em, because Heading_Font is an extra-wide face whose default letterforms already carry generous horizontal space and would read as over-tracked under positive letter-spacing.
8. THE Site SHALL declare a `line-height` for the intro `h1` of at least 1.05 and at most 1.20, and a `line-height` for `h2` through `h6` of at least 1.20 and at most 1.50.
9. THE Site SHALL preserve the existing `text-transform: uppercase` treatment of Heading_Text and the existing `color` resolution of Heading_Text through the `fg-bold` palette value.
10. WHILE the viewport width matches a breakpoint at or below `large`, THE Site SHALL scale Heading_Text through the existing root `font-size` steps of 12pt at `<=xlarge`, 11pt at `<=large`, and 10pt at `<=xxsmall`, keeping the rem values of criteria 5 and 6 unchanged.
11. THE Site SHALL render every Heading_Text element on all nine Content_Pages at viewport widths of 320px, 768px, 1024px, and 1440px with no character visually truncated by its containing block and with the document producing no horizontal scrollbar.
12. THE Site SHALL render the intro `h1` text "Jeffery Ross" on a single line at viewport widths of 768px, 1024px, and 1440px.
13. THE Site SHALL render the intro `h1` text "Jeffery Ross" on at most two lines at a viewport width of 320px, with no character truncated and no horizontal scrollbar.
14. THE Site SHALL render Heading_Text at a Contrast_Ratio of at least 4.5:1 against the background of its containing block on all nine Content_Pages.
15. THE Webfont_Bundle `unicode-range` declared for Heading_Font under Requirement 2 criterion 9 SHALL fall within Heading_Font's Basic Latin, Latin-1 and Latin Extended coverage.
16. IF a Heading_Text string contains a character absent from Heading_Font, THEN THE Site SHALL render that character from the first Fallback_Stack family that provides it, without substituting the whole string.

### Requirement 4: Grotesque Typeface for Body Text

**User Story:** As a visitor reading project descriptions, I want body text rendered in a clean modern grotesque, so that longer passages remain comfortable to read.

The free Telegraf tier supplies the complete glyph set but only a selection of styles, so the availability of a true 700 face is not yet established. The weight criteria below are written against the styles actually present in the licensed download.

#### Acceptance Criteria

1. THE Compiled_Stylesheet SHALL declare, for every rule that sets a `font-family` for Body_Text, a stack beginning with Body_Font.
2. THE Site SHALL render all Body_Text on every Content_Page in Body_Font.
3. THE Compiled_Stylesheet SHALL declare, for Body_Text and for its bold treatment, only `font-weight` values that are weights of Body_Font faces present in the licensed download and included in the Webfont_Bundle.
4. IF the licensed Body_Font download provides no bold face, THEN THE Site SHALL declare the bold `font-weight` equal to the body `font-weight`, SHALL apply an alternative emphasis treatment to `strong` and `b` elements that does not rely on a browser-synthesized bold, and SHALL record the limitation in the Readme_Credits section of `README.md` or in the Sync_Document. **Amended in Change Set 3** only to name the two permitted locations, because Change Set 3 shortens `README.md` and moves its long-form notes to the Sync_Document. The branch itself does not fire: a true bold face ships (Assumptions item 6), so no limitation note is required in either location.
5. THE Site SHALL declare a single `line-height` value for Body_Text in the inclusive range 1.6 to 1.9, replacing the existing value of 2.375, because Body_Font has a larger x-height than the serif typeface it replaces.
6. THE Site SHALL declare a `font-size` of 1rem for Body_Text and SHALL preserve the existing root `font-size` values of 16pt at the default breakpoint, 12pt at `<=xlarge`, 11pt at `<=large`, and 10pt at `<=xxsmall`.
7. THE Site SHALL render Body_Text at a Contrast_Ratio of at least 4.5:1 against the background of its containing block.
8. THE Site SHALL render Monospace_Text in the existing `family-fixed` stack.
9. THE Site SHALL render project card description text within the bounds of the card, with no text clipped by the card boundary and no horizontal page scrollbar, at viewport widths of 320px, 768px, 1024px, and 1440px.
10. THE Site SHALL preserve the `p { text-align: justify }` declaration in the Compiled_Stylesheet.
11. WHERE the Webfont_Bundle omits a true italic Body_Font face, THE Site SHALL render `em` and `i` elements in the Body_Font family with a synthesized oblique slant and SHALL NOT substitute a family from the Fallback_Stack for those elements.
12. WHEN Body_Text is enclosed in a `strong` or `b` element, THE Site SHALL render that text at the bold `font-weight` permitted by criterion 3, or apply the alternative treatment defined by criterion 4.
13. THE Site SHALL render Body_Text at a computed font size of at least 13px at viewport widths of 320px and above.
14. IF a Body_Text string contains a character absent from Body_Font, THEN THE Site SHALL render that character from the first Fallback_Stack family that provides it.

### Requirement 5: Legibility of Small Interface Text

**User Story:** As a visitor scanning skill tags and the copyright line, I want small interface text to stay readable, so that the compact labels remain useful.

Heading_Font is an extra-wide display face with tight, closed apertures, which collapse into one another well before the smallest Chrome_Text size is reached. Chrome_Text currently inherits the heading font at sizes as small as 0.55rem, where Heading_Font would be markedly less legible than a normal-width display face. Its extra-wide letterforms also consume far more horizontal space per character, which makes single-line skills-button labels harder to fit and gives added weight to criteria 4 and 7 below. All Chrome_Text is therefore routed to Body_Font.

#### Acceptance Criteria

1. THE Site SHALL render every Chrome_Text element — skills button labels, Read More button labels, form labels, pagination links, table headers, navigation panel links, and the `#copyright` block — in Body_Font, and SHALL NOT resolve any Chrome_Text element to Heading_Font.
2. THE Site SHALL render Chrome_Text at a `font-weight` equal to the `usWeightClass` of a Body_Font face present in the Webfont_Bundle, and SHALL NOT depend on a browser-synthesized intermediate or emboldened weight. This criterion is weight-agnostic and reads correctly for both cases now in use: Bold_Chrome_Text resolves to 800, matching the shipped `PPTelegraf-Ultrabold.otf` face, and all remaining Chrome_Text resolves to 400, matching the shipped `PPTelegraf-Regular.otf` face. Requirement 11 fixes which elements take which of the two weights.
3. THE Site SHALL render every Chrome_Text element at a computed font size of at least 0.7rem (11.2px at a 16px root font size) at each of the viewport widths 320px, 768px, 1024px, and 1440px.
4. THE Site SHALL render each skills button label of 20 characters or fewer on a single line within the bounds of its project card, with no clipped characters and no horizontal overflow, at viewport widths of 320px, 768px, 1024px, and 1440px.
5. THE Site SHALL preserve the existing uppercase transform and background treatment of the skills and Read More buttons.
6. THE Site SHALL render Chrome_Text at a Contrast_Ratio of at least 4.5:1 against the background of its containing block in both its default state and its hover state.
7. IF a skills button label exceeds the single-line width available inside its project card at any of the viewport widths 320px, 768px, 1024px, or 1440px, THEN THE Site SHALL wrap the label onto additional lines inside the card bounds, keeping every character visible and producing no horizontal page overflow.
8. THE Site SHALL declare a `letter-spacing` for uppercase Chrome_Text between 0.025em and 0.075em inclusive.

### Requirement 6: Font Loading Resilience

**User Story:** As a visitor on a slow or restricted connection, I want the page to remain readable when a custom font does not load, so that I can still read the content.

Heading_Font is extra-wide, so its advance widths exceed those of every commonly installed fallback family by a wide margin. The fallback-to-webfont swap will therefore shift heading layout more than a normal-width display face would, and the swap criteria below are written to bound that heightened reflow risk.

#### Acceptance Criteria

1. THE Site SHALL declare a Fallback_Stack of at least two families after Heading_Font in every `font-family` declaration naming Heading_Font, where each named family is installed by default on at least one of Windows, macOS, iOS, or Android, and where the families are ordered so that the widest available faces are named first as the closest match to Heading_Font's advance widths.
2. THE Site SHALL declare a Fallback_Stack of at least two families after Body_Font in every `font-family` declaration naming Body_Font, where each named family is installed by default on at least one of Windows, macOS, iOS, or Android.
3. THE Site SHALL terminate each Fallback_Stack for Heading_Font and Body_Font with the CSS generic family keyword `sans-serif`.
4. IF a Webfont_Bundle file request returns an error response, is blocked, or does not complete within 3 seconds, THEN THE Site SHALL render the affected text in the first available family of its Fallback_Stack.
5. IF a Webfont_Bundle file fails to load, THEN THE Site SHALL render all page content at the same `font-size`, `font-weight`, `line-height`, and colour values declared for the webfont case, with no text hidden, clipped, or truncated.
6. WHILE a Webfont_Bundle file is downloading, THE Site SHALL render the affected text in its Fallback_Stack from the first paint onward, with an invisible-text block period of 0 ms.
7. WHILE the Fallback_Stack is in use, THE Site SHALL render every Content_Page without horizontal overflow and SHALL keep every heading, paragraph, and skills button label within the bounds of its containing block, at viewport widths of 320px, 768px, 1024px, and 1440px.
8. WHEN the Heading_Font file finishes loading after fallback text has been painted, THE Site SHALL replace the fallback rendering with Heading_Font without introducing horizontal overflow, without truncating any Heading_Text character, and while keeping the intro `h1` text "Jeffery Ross" on a single line at viewport widths of 768px and above, notwithstanding the larger advance-width increase produced by an extra-wide face.
9. WHEN the Body_Font file finishes loading after fallback text has been painted, THE Site SHALL replace the fallback rendering with Body_Font without introducing horizontal overflow and without clipping project card description text.
10. IF a Webfont_Bundle file fails to load, THEN THE Site SHALL present no error message, empty text run, or placeholder glyph to the visitor for the affected text.
11. IF every family in a Fallback_Stack is unavailable, THEN THE Site SHALL render the affected text using the terminating generic family keyword at the declared sizes and weights.

### Requirement 7: Single Stylesheet Source and Its Maintenance Note

**User Story:** As the site owner maintaining the repository, I want one stylesheet that is unambiguously the source of what browsers execute, together with a short note recording the things about it that break the site quietly, so that a future edit does not silently undo this refresh.

**Rewritten in Change Set 4.** This requirement was *Stylesheet Source and Compiled Output Parity*, and its premise was two artifacts that had to agree: the SASS tree under `assets/sass/` and the hand-mirrored `assets/css/main.css`. **The tree has been deleted, so the premise is gone** and every parity criterion with it. What is deleted, and why:

| Former criterion | Disposition |
|---|---|
| c1 — the Stylesheet_Source declares the required values | **Folded into new criterion 1**, which puts the obligation on the one artifact that remains. |
| c2 — the Compiled_Stylesheet declares the same values plus the `@font-face` rules | **Kept as new criterion 1.** |
| c3 — every declaration in the CSS equals the value the SASS resolves | **Deleted.** There is no second artifact to differ from. Every criterion elsewhere in this document that cited "Requirement 7 criterion 3" now names the Compiled_Stylesheet directly. |
| c4 — every `font-family` resolves through the `$font` map, with no per-rule literals | **Deleted, and nothing replaces it.** It asserted a *mechanism* and no observable behaviour: `main.css` has always carried resolved literals at every site, so restating it against `main.css` would forbid the only form the artifact can take. What it actually protected — one family stack everywhere, and no stray typeface name — is criterion 1 here plus Requirement 3 criteria 2 and 3, Requirement 4 criteria 1 and 2, and Requirement 8 criteria 1 through 3. |
| c5, c11–c13 — the documented procedure, its README link, its retained content, and the defect rule | **Retained as criteria 6 through 9, rewritten**: the Sync_Document is now a maintenance note for `main.css`, not a regeneration procedure. |
| c6, c7, c9, c10 | **Retained unchanged in substance** as criteria 2, 3, 4 and 5. |
| c8 — a SASS edit must ship with its CSS mirror before deploy | **Deleted.** There is nothing to mirror. |

The obligation that survives is narrower and more honest than the one it replaces: not "keep two files equal", but "there is exactly one stylesheet, and here are the five things about it that a maintainer cannot infer by reading it".

#### Acceptance Criteria

1. THE Compiled_Stylesheet SHALL declare the `font-family`, `font-size`, `font-weight`, `line-height`, and `letter-spacing` values required for Heading_Text, Body_Text, and Chrome_Text by Requirements 3, 4, and 5, the Footer_Email_Link colour and hover colour required by Requirement 1, and one `@font-face` rule for each file in the Webfont_Bundle as required by Requirement 2.
2. THE Site SHALL retain the `@import url(fontawesome-all.min.css)` declaration on the first line of the Compiled_Stylesheet, SHALL place every `@font-face` rule below it, and SHALL render every Font Awesome icon on all nine Content_Pages with no missing-glyph substitution. **Change Set 4 note:** the Noscript_Stylesheet's `@import url(font-awesome.min.css)` was removed, because no such file exists in the repository and the real icon stylesheet is already imported here.
3. THE Site SHALL retain every existing Font Awesome webfont file under `assets/webfonts/` unchanged in filename, count, and file content.
4. THE Compiled_Stylesheet SHALL contain zero occurrences of the family names `Merriweather` and `Source Sans Pro`.
5. THE Site SHALL declare all Heading_Text, Body_Text, and Chrome_Text `font-family`, `font-size`, `font-weight`, and `line-height` values in the Compiled_Stylesheet only, and SHALL contain zero inline `style` attributes and zero in-page `<style>` blocks declaring those four properties on any of the nine Content_Pages.
6. THE Site SHALL document, in `README.md` or in the Sync_Document, a maintenance note for the Compiled_Stylesheet that records each item listed in criterion 8, names the file each item concerns, and states how each item is verified.
7. WHERE the note required by criterion 6 is held in the Sync_Document, THE Site SHALL link the Sync_Document from `README.md` in a single line of body text, so that a maintainer who opens `README.md` reaches the note in one step.
8. THE Site SHALL retain in the note required by criterion 6 these five items, each in its own position: the `@import` / `@font-face` ordering item, including the consequence that a rule above line 1 invalidates the Font Awesome import and removes every icon on all nine pages; the zero-occurrence item covering `Merriweather`, `Source Sans Pro`, the superseded footer link colour `#4a5158`, and `scroll-behavior` / `prefers-reduced-motion` with the comments-stripped scanning instruction; the smooth-scroll item carrying the measured first-movement figures; the last-declaration-wins item, including that the first of the two `#copyright` `color` declarations is the opaque `#ffffff` and the second is the value that paints; and the per-page Copyright_Block markup item naming the three pages that write the `div` on one source line and the byte-identity check across all nine.
9. IF the note required by criterion 6 is absent from both `README.md` and the Sync_Document, or is present in a form that omits an item, a file name, or a verification instruction listed in criterion 8, THEN THE Site SHALL be treated as failing this requirement, and the omission SHALL be reported as a defect naming the missing item, file, or instruction.

### Requirement 8: Cross-Page Visual Consistency

**User Story:** As a visitor navigating between the project pages, I want typography to look the same everywhere, so that the site feels like one coherent portfolio.

#### Acceptance Criteria

1. THE Site SHALL render, for each of `h1` through `h6`, the same computed Heading_Text font-family stack, font-weight, font-size, and letter-spacing on all nine Content_Pages, with no difference in computed font-size greater than 0.01rem, measured at viewport widths of 320px, 768px, 1024px, and 1440px.
2. THE Site SHALL render the same computed Body_Text font-family stack, font-weight, font-size, and line-height on all nine Content_Pages, with no difference in computed font-size greater than 0.01rem and no difference in computed line-height greater than 0.05, measured at viewport widths of 320px, 768px, 1024px, and 1440px.
3. THE Site SHALL render the same computed Chrome_Text font-family stack and font-weight on all nine Content_Pages, and SHALL render each Chrome_Text class of element (skills button labels, Read More button labels, form labels, pagination links, table headers, navigation panel links, and the `#copyright` block) at the same computed font-size on every Content_Page on which that element appears, measured at viewport widths of 320px, 768px, 1024px, and 1440px.
4. THE Site SHALL link every Content_Page to the Compiled_Stylesheet `assets/css/main.css` as its only typography source, and SHALL declare no inline `style` attribute and no embedded `<style>` rule on any Content_Page that sets `font-family`, `font-size`, `font-weight`, `line-height`, or `letter-spacing` for Heading_Text, Body_Text, or Chrome_Text.
5. THE Site SHALL preserve on each Content_Page the same set, count, order, and nesting of the intro block, navigation, project card, footer contact section, and footer social icon elements that the page contained before this change, adding and removing no such element, and SHALL preserve the Copyright_Block itself together with its position within the footer. **The content inside the Copyright_Block is exempt from this criterion**, because Requirement 13 replaces the "Fonts & icons" list item with the Back_To_Top_Control and the Design_Credit; Requirement 13 criteria 12 and 13 govern that inner markup instead, and the exemption reaches no element outside the Copyright_Block.
6. WHEN a Content_Page finishes loading, THE Site SHALL initialise and animate the water particle canvas defined in `assets/js/waterParticles.js` and SHALL respond to project card hover, expand, and Read More interactions defined in `assets/js/projectCards.js`, with no uncaught script error reported by the browser.
7. THE Site SHALL preserve every colour value declared in the Compiled_Stylesheet unchanged, except the single Footer_Email_Link colour value, which is `#3a4148` to satisfy Requirement 1 criteria 12 and 13. The Copyright_Block recolouring required by Requirement 14 does **not** widen this exception: it changes the alpha of the white the `#copyright` rule already declared, and the opaque white `#ffffff` that the surrounding rules resolve to stays unchanged. **Amended in Change Set 4** to read against the one artifact that declares colours; the exception count is unchanged at one, and "palette value" now means "a colour literal in `main.css`" rather than an entry in a deleted variable map.
8. THE Site SHALL preserve the exact `href` value of every navigation link and project link on all nine Content_Pages, and each internal `href` SHALL resolve to a file present in the deployed repository.
9. IF a Content_Page renders Heading_Text or Body_Text whose computed font-family stack differs from the stack resolved by the Compiled_Stylesheet for the same element type and viewport width, THEN THE Site SHALL be treated as failing this requirement and the discrepancy SHALL be reported as a defect identifying the affected page and element, with no other Content_Page modified.

### Requirement 9: Font Licence Compliance and Provenance

**User Story:** As the site owner publishing a public repository, I want the licence terms of both typefaces satisfied and their origin documented, so that the Site stays within its free personal-use grants and the fonts are known to be the designers' unmodified releases.

Both fonts are used under free personal-use grants that depend on the Site remaining Non_Commercial_Use. Provenance matters independently of licensing: aggregator mirror sites may ship files that have been altered, subsetted, or repackaged, and may restate licence terms that no longer match the designer's own.

**Amended in Change Set 2:** no licence or EULA text file for Heading_Font could be located from the designer's channels, so criterion 2's stored-text obligation now applies to Body_Font only and criteria 9 through 11 define a recorded-fields obligation for Heading_Font in its place. The substantive licence terms are untouched — see criterion 10.

#### Acceptance Criteria

1. THE Site SHALL obtain every Heading_Font and Body_Font file in the Webfont_Bundle from the designer's or foundry's official distribution channel, and SHALL obtain no such file from an aggregator or mirror download site.
2. THE Site SHALL store, alongside the font files under `assets/webfonts/`, the licence or end-user licence agreement text supplied by the foundry for Body_Font, and SHALL record the download source URL and download date for every Heading_Font and Body_Font file in the Provenance_Record. The Body_Font obligation is satisfied by the existing `assets/webfonts/EULA-PangramPangram-FreeForPersonalUse-MAY2021.pdf`. **This criterion no longer requires a stored licence text file for Heading_Font**; criterion 9 states what is required for Heading_Font instead.
3. THE Site SHALL credit Heading_Font to Alberto Fontense and Body_Font to Pangram Pangram Foundry, each with its licence tier, in the Readme_Credits section of `README.md`. **Amended in Change Set 3** to permit a compact bullet form: a single `**Fonts:**` bullet naming both typefaces, both designers, and both licence tiers satisfies this criterion, and no minimum length, prose form, or accompanying licence explanation is required. The obligation is that four facts are present and visible for each typeface — the typeface name, which text it renders, its designer or foundry, and its licence tier. The obligation itself is unchanged; only the permitted form is relaxed.
4. THE Site SHALL restrict its content to Non_Commercial_Use, and SHALL present no advertisement of paid services, no statement of freelance or contract availability, no rates or pricing, no sponsorship or affiliate content, and no other monetisation, on any of the nine Content_Pages, because both font licences are conditioned on non-commercial use.
5. IF the purpose of the Site changes to include commercial use, THEN THE Site SHALL obtain a paid licence for each affected typeface before the next deployment, including for Body_Font a Pangram Pangram Web licence covering the deployed domain and the anticipated monthly pageview tier.
6. THE Site SHALL ship every Heading_Font and Body_Font file in its Vendor_Supplied_Format, byte-for-byte as the designer or foundry supplied it, and SHALL apply no format conversion, subsetting, renaming of internal font data, or other modification to any font file. Shipping Body_Font unconverted is the load-bearing condition of the delivery path chosen in Requirement 2 criterion 3, not merely a precaution: the free Telegraf tier grants no converted format, so a converted Body_Font file would leave the Site with no licensed delivery route at all.
7. WHERE the Body_Font delivery path recorded in Requirement 2 criterion 3 is later abandoned in favour of a converted file, THE Site SHALL first obtain written permission to convert from the foundry and store that permission in the repository alongside the licence text required by criterion 2, before shipping any converted file.
8. WHEN a font file in the Webfont_Bundle is added or replaced, THE Site SHALL record the source URL, download date, licence tier, and file format for that file in the Provenance_Record.
9. THE Site SHALL record in the Provenance_Record, for Heading_Font, the licence tier "free for personal use", the designer name, the download source URL, and the download date, and SHALL treat those four recorded fields as the complete provenance obligation for Heading_Font in place of a stored licence text file, because no vendor-supplied licence or EULA text for Horizon is obtainable.
10. THE Site SHALL remain bound by the Heading_Font free-personal-use terms whether or not a copy of those terms is stored in the repository. The absence of a stored licence text file narrows the record, not the obligation: Heading_Font stays usable only while the Site satisfies Non_Commercial_Use, so criteria 4 and 5 apply to Heading_Font exactly as they did before this amendment, and Requirement 9's non-commercial constraint is unaffected.
11. THE Site SHALL state in the Provenance_Record, in place of the pre-amendment `TODO (owner)` note under the `Horizon.woff2` record, that no vendor licence text is available for Heading_Font and that this is an accepted and recorded position rather than an outstanding action, and SHALL set that record's `licence_text_file` field to a value denoting "none — accepted, see note" rather than to a filename. THE Site SHALL paraphrase rather than reproduce the designer's terms, and SHALL invent no substitute licence file.

### Requirement 10: Centred Project Card Titles

**User Story:** As a visitor scanning the homepage project grid, I want each card title centred in its header band, so that the cards read as a symmetrical, deliberate grid rather than as left-ragged blocks.

The Card_Header_Band currently declares `text-align: left`, which the Card_Heading inherits. The band contains the Card_Heading and nothing else, so the change is confined to that one declaration and its cascade. Several card titles wrap: "KillerByte / Full-body Spinner Battlebot" carries an explicit `<br />` inside its `h2 > a`, and other titles wrap naturally at narrow viewports. Centring must therefore hold per rendered line, not merely for a single-line case.

#### Acceptance Criteria

1. THE Site SHALL render every Card_Heading horizontally centred within the content box of its Card_Header_Band on `index.html`, such that the distance from the leftmost rendered glyph edge to the band's left content-box edge and the distance from the rightmost rendered glyph edge to the band's right content-box edge differ by at most 1 CSS pixel.
2. WHERE a Card_Heading renders on two or more lines, THE Site SHALL centre each rendered line independently within the Card_Header_Band content box, such that for every line the left and right gaps described in criterion 1 differ by at most 1 CSS pixel.
3. WHEN a Card_Heading contains an explicit `<br />` element, THE Site SHALL centre each of the resulting lines as required by criterion 2, with the "KillerByte" line and the "Full-body Spinner Battlebot" line each independently centred.
4. THE Site SHALL preserve the Card_Heading declared `font-size` of 1.1rem, `text-transform: none`, `line-height`, and `color` value.
5. THE Site SHALL preserve the resolution of the `h2 > a` anchor colour by inheritance from the Card_Heading, declaring no separate colour value on that anchor and producing no visible colour difference between a linked and an unlinked Card_Heading.
6. THE Site SHALL render the centred Card_Heading with every character fully visible inside the Card_Header_Band and with the document producing no horizontal scrollbar, at viewport widths of 320px, 768px, 1024px, and 1440px.
7. THE Site SHALL restrict the centring to the Card_Heading and the Card_Header_Band, and SHALL leave the `text-align` resolution of every other element on all nine Content_Pages — including the two further `text-align: left` declarations on the `body.home #main > .posts > article p` card-description rules in the Compiled_Stylesheet — identical to its pre-change value.
8. THE Site SHALL declare the centring in the Compiled_Stylesheet, and SHALL introduce no inline `style` attribute and no in-page `<style>` block on any Content_Page to achieve it.
9. THE Site SHALL preserve the Card_Header_Band `background-color`, `padding`, and box dimensions, so that centring the title changes no card height and no grid alignment.

### Requirement 11: Bold Weight for Navigation, Buttons, and Skills Pills

**User Story:** As the site owner, I want the top banner links, the action buttons, and the skills pills set in Telegraf Ultrabold, so that the interactive chrome reads as deliberately heavier than the body copy.

Three groups of Chrome_Text move from the body weight (400) to the bold weight (800). The 800 face already ships as `assets/webfonts/PPTelegraf-Ultrabold.otf` and is already declared at `font-weight: 800` under the `PP Telegraf` CSS family, so no new font file is added and the Requirement 2 bundle budget is unaffected. The free Telegraf download contains no 700 face, which is why 800 rather than 700 is the bold weight.

Ultrabold's advance widths exceed Regular's, so every label in these groups gets wider at an unchanged font size. That is a real layout risk for the top navigation bar, for the fixed-width `.button` boxes, and above all for the 0.55rem homepage skills pills, which are the smallest Chrome_Text on the Site. Criteria 7 through 11 bound that risk; Requirement 12 handles the pill box geometry that the extra width disturbs.

#### Acceptance Criteria

1. THE Site SHALL render every Bold_Chrome_Text element at a computed `font-weight` of 800 on all nine Content_Pages: the top navigation links selected by `#nav ul.links` and their `a` descendants, every `.button` label including the Read More links and the View Model `.button.primary`, `.button.primary.small`, and `.button.primary.small.fit` variants, and every Skills_Pill label.
2. **Deleted in Change Set 4.** This criterion required the weight to resolve through the `weight-bold` value in the `$font` map in `assets/sass/libs/_vars.scss` and forbade per-rule literal weight numbers outside that map. Both clauses describe a **mechanism that no longer exists** and neither describes anything observable: with the map deleted, every `font-weight` in `main.css` is necessarily a literal, so the second clause would forbid the only form the artifact can take. Nothing is lost — criterion 1 already requires the computed weight to be 800 at every Bold_Chrome_Text element on all nine pages, and criterion 6 requires it to match a shipped face. The criterion number is retained rather than reused so that references elsewhere in this document and in `design.md` stay valid.
3. THE Site SHALL satisfy criterion 1 using a single `.button` rule that covers the Read More links, the View Model variants, and the Skills_Pill labels together, so that no `.button` variant is left at the body weight.
4. THE Site SHALL serve the 800 weight from the `PPTelegraf-Ultrabold.otf` face already present in the Webfont_Bundle, and SHALL add no font file, remove no font file, and change no `@font-face` rule in satisfying this requirement.
5. THE Site SHALL resolve the computed `font-family` of every Bold_Chrome_Text element to a stack whose first family is Body_Font, leaving the family unchanged from its pre-amendment value.
6. THE Site SHALL render every Bold_Chrome_Text element at a computed `font-weight` equal to the `usWeightClass` of a face present in the Webfont_Bundle, and SHALL permit no browser-synthesized or interpolated weight for any Bold_Chrome_Text element, satisfying Requirement 5 criterion 2.
7. THE Site SHALL render every Bold_Chrome_Text label at the smallest `font-size` declared for any Bold_Chrome_Text element — 0.55rem for the homepage Skills_Pill labels — with no two adjacent glyph outlines overlapping or touching, and with every enclosed counter shape remaining open rather than filled, at viewport widths of 320px, 768px, 1024px, and 1440px.
8. THE Site SHALL declare a `letter-spacing` for uppercase Bold_Chrome_Text within the 0.025em to 0.075em range required by Requirement 5 criterion 8, and SHALL declare a value no smaller than the value declared for the same element before this amendment, because the heavier face reduces the white space between glyphs at a constant tracking value.
9. THE Site SHALL render every Bold_Chrome_Text label wholly within the bounds of its containing element at viewport widths of 320px, 768px, 1024px, and 1440px, with no character clipped by that element's box, no ellipsis or other truncation indicator applied, and no horizontal page scrollbar produced.
10. THE Site SHALL render the top navigation link labels "Projects" and "CAD Gallery" each on a single line within `#nav`, with no navigation link overlapping another and no navigation link extending past the `#nav` content box, at viewport widths of 320px, 768px, 1024px, and 1440px.
11. THE Site SHALL render the "Read More" and "View Model" labels each on a single line wholly inside the padding box of their `.button` element at viewport widths of 320px, 768px, 1024px, and 1440px, retaining the `white-space: nowrap` declaration on `.button` and adjusting the button box or its padding rather than allowing the label to extend past the button box.
12. IF a Bold_Chrome_Text label cannot satisfy criteria 9, 10, or 11 at its declared `font-size` and the criterion 8 `letter-spacing`, THEN THE Site SHALL enlarge the containing element or its padding, and SHALL NOT reduce the declared `font-size` below the floor set by Requirement 5 criterion 3, SHALL NOT return the element to the body weight, and SHALL NOT apply `text-overflow` truncation.
13. THE Site SHALL preserve for every Bold_Chrome_Text element its pre-amendment `text-transform`, `background-color`, `border`, `border-radius`, default and hover `color` values, and hover transition timing.
14. THE Site SHALL render every Bold_Chrome_Text element at a Contrast_Ratio of at least 4.5:1 against the background of its containing block in both its default and its hover state, satisfying Requirement 5 criterion 6 at the heavier weight.
15. THE Site SHALL leave every Chrome_Text element outside Bold_Chrome_Text — form labels, pagination links, table headers, and the Copyright_Block — at its computed `font-weight` of 400. **Amended in Change Set 3:** the Nav_Panel_Link elements are removed from this list, and both the Nav_Panel_Link elements and the Nav_Panel_Toggle join Bold_Chrome_Text under Requirement 16, which moves them to 800. The four groups still named here — form labels, pagination links, table headers, and the Copyright_Block — remain at 400, and this criterion continues to bind them exactly as it did before the amendment. Nothing else moves weight: the amendment reaches the two navigation panel sites and no other element.
16. WHEN the design phase fixes the Bold_Chrome_Text declarations, THE Site SHALL record in the design document, for the top navigation links, the Read More and View Model labels, and the widest Skills_Pill label, the measured rendered label width at the body weight and at the bold weight at each of the viewport widths 320px, 768px, 1024px, and 1440px, so that the advance-width increase is established by measurement rather than assumed.

### Requirement 12: Skills Pill Box Fit at the Bold Weight

**User Story:** As a visitor looking at a project card, I want each skills pill's box to fit its label snugly and evenly, so that the pills read as a tidy row of tags rather than as boxes that mismatch their text.

This requirement depends on Requirement 11: the bold weight widens every Skills_Pill label, and the owner reports the pill boxes now mismatch their text. Two geometries are in scope. The homepage geometry declares `font-size: 0.55rem`, `height: auto`, `min-height: 1.7rem`, `line-height: 1.4`, `padding: 0.15rem 0.4rem`, `border-radius: 999px`, `white-space: normal`, and a 1px border. The wider-context geometry declares `font-size: 0.7rem`, `height: 2.25rem`, `line-height: 2.25rem`, and `padding: 0 1rem`, and so centres its label vertically through `line-height` against a fixed `height`.

The criteria below are expressed as measurable relationships between the label box and the pill box rather than as pixel values. **No exact pixel or rem value is pinned here** — the design phase measures the labels at the bold weight and derives the values that satisfy these bounds.

**Change Set 4 note — the wider-context geometry no longer has a declaration site.** Its rule, the grouped `body.home #main .button.skills, body.home #main .actions .button`, had **zero rendered instances**: both halves were overridden in full by later, equally specific rules, and only `index.html` carries a Skills_Pill at all. It was kept solely because the verification harness read its declared values, and Change Set 4 deleted it together with that reader. **No observable behaviour changes**, because no element ever resolved to it. Criteria 1 through 9, 11 and 12 continue to bind every Skills_Pill that renders — all of which take the homepage geometry — and criterion 10's wider-context arm has nothing left to measure. It is retained unamended so that a future rule reintroducing a second geometry inherits the same bounds.

#### Acceptance Criteria

1. THE Site SHALL render each Skills_Pill label's rendered text box wholly inside the pill's padding box at viewport widths of 320px, 768px, 1024px, and 1440px, with no glyph clipped by the pill's border box and no part of the label painted outside that border box.
2. THE Site SHALL render each Skills_Pill so that the vertical distance from the top of the label's first line box to the pill's content-box top edge and the vertical distance from the bottom of the label's last line box to the pill's content-box bottom edge differ by at most 1 CSS pixel, at each of the viewport widths named in criterion 1.
3. THE Site SHALL render each Skills_Pill so that the horizontal distance from the leftmost rendered glyph edge to the pill's content-box left edge and the distance from the rightmost rendered glyph edge to the pill's content-box right edge differ by at most 1 CSS pixel, at each of the viewport widths named in criterion 1.
4. THE Site SHALL declare each Skills_Pill's horizontal padding at a value between 1.5 times and 3.5 times inclusive its effective vertical gap, where the effective vertical gap is the declared vertical padding for the homepage geometry and is half the difference between the declared `height` and the rendered line box height for the wider-context geometry, so that the pill reads as a horizontally extended capsule with balanced padding on all four sides rather than as a box padded unevenly.
5. WHERE a Skills_Pill label renders on a single line, THE Site SHALL keep the ratio of the label's rendered text width to the pill's border-box width at no less than 0.40 and no greater than 0.88, and the ratio of the label's rendered line box height to the pill's border-box height at no less than 0.40 and no greater than 0.85, at each of the viewport widths named in criterion 1.
6. WHERE a Skills_Pill label renders on two or more lines, THE Site SHALL keep the ratio of the sum of the label's rendered line box heights to the pill's border-box height at no greater than 0.90, at each of the viewport widths named in criterion 1.
7. THE Site SHALL declare each Skills_Pill's `min-height` at a value no smaller than the sum of one rendered line box height, the declared top and bottom padding, and the declared top and bottom border widths, so that a single-line label can never be vertically clipped by the pill's minimum box.
8. THE Site SHALL keep the wrap behaviour required by Requirement 5 criterion 7 intact, retaining `white-space: normal` and an automatic `height` on the homepage geometry, wrapping an over-wide label onto additional lines inside its project card with every character visible, and producing no horizontal page overflow.
9. THE Site SHALL preserve each Skills_Pill's `border-radius: 999px` capsule silhouette, `background-color`, `border` colour and width, and label `color`, so that the box correction changes fit and not appearance.
10. THE Site SHALL apply criteria 1 through 7 to both the homepage geometry and the wider-context geometry, measuring each at its own declared `font-size`.
11. IF a Skills_Pill cannot satisfy criteria 1 through 6 at any of the viewport widths named in criterion 1, THEN THE Site SHALL adjust that pill's declared padding, `min-height`, or `line-height`, and SHALL NOT reduce the declared `font-size` below the floor set by Requirement 5 criterion 3, SHALL NOT reintroduce `white-space: nowrap`, and SHALL NOT reintroduce a fixed `height` on the homepage geometry that clips a second line.
12. THE Site SHALL render every Skills_Pill in a project card's skills row without any pill overlapping another pill and without any pill extending past the card's content box, at each of the viewport widths named in criterion 1.
13. WHEN the design phase fixes the Skills_Pill geometry, THE Site SHALL record in the design document, for the narrowest and the widest Skills_Pill label of each geometry, the measured label text box width and height and the measured pill border-box width and height at each of the viewport widths named in criterion 1, together with the resulting ratios required by criteria 5 and 6, so that the fit is established by measurement rather than asserted.

### Requirement 13: Footer Back to Top Control with Retained Design Credit

**User Story:** As a visitor who has scrolled to the bottom of a long project page, I want a Back to top control in the footer, so that I can return to the navigation without scrolling back by hand.

All nine Content_Pages currently carry, inside the Copyright_Block, the markup `<ul><li>Fonts &amp; icons: <a href="https://html5up.net">HTML5 UP</a></li></ul>`. `vexlego.html` writes the ampersand as the `&amp;` entity, and `killerbyte.html`, `launchtoy.html`, and `vexlego.html` write the whole `#copyright` div on one source line. That list item becomes a Back to top control.

**The HTML5 UP attribution is retained.** The [HTML5 UP licence](https://html5up.net/license) places its templates under Creative Commons Attribution 3.0: personal use, commercial use, and modification are all expressly permitted, and the credit is the stated consideration given in exchange. HTML5 UP additionally sells attribution-free usage separately through Pixelarity, which confirms that the credit is the price of the free tier rather than a courtesy. The Site is also still substantially template-derived. **The evidence for that was restated in Change Set 4**, because the 24 `assets/sass/**` files that used to carry the Massively header have been deleted — which changes what the derivation is *shown by* and not whether it holds: both shipped stylesheets (`assets/css/main.css` and `assets/css/noscript.css`) carry the Massively header, as does every one of the nine Content_Pages and `assets/js/main.js`; `util.js`, `breakpoints.min.js`, `browser.min.js`, `jquery.scrollex.min.js`, and `jquery.scrolly.min.js` are template files that still ship; and the `#wrapper`, `is-preload`, `split contact`, `icons`, and `actions` structures appear on all nine pages. Deleting a derived file does not make the remaining ones original. The degree of visual and functional divergence from the published Massively demo does not discharge the attribution condition, because CC BY 3.0 attaches attribution to derivative works as well as to verbatim copies. What does change is the wording: "Fonts & icons" is no longer accurate now that Heading_Font and Body_Font are Horizon and Telegraf, so the credit is reworded as a design credit. *Licence terms above are summarised, not quoted; content was rephrased for compliance with licensing restrictions, and the linked licence page is authoritative.*

#### Acceptance Criteria

1. THE Site SHALL render a Back_To_Top_Control inside the Copyright_Block on all nine Content_Pages.
2. WHEN a visitor activates the Back_To_Top_Control with a pointer, THE Site SHALL bring the top of the current document into the viewport, leaving the visitor on the same Content_Page and initiating no page load of a different document.
3. WHEN a visitor activates the Back_To_Top_Control with the keyboard while the control holds focus, THE Site SHALL produce the same outcome required by criterion 2.
4. THE Site SHALL implement the Back_To_Top_Control as an `<a>` element whose `href` is a same-document fragment reference that resolves to the top of the document, being either `#top` — which browsers treat as the document top whether or not an element with that `id` exists — or `#wrapper`, whose target element is present on all nine Content_Pages.
5. IF scripting is disabled in the visitor's browser, or `assets/js/jquery.scrolly.min.js` fails to load, or any other script on the page fails to execute, THEN THE Site SHALL still satisfy criteria 2 and 3 through the fragment navigation required by criterion 4.
6. THE Site SHALL declare no `href="#"` value, no `javascript:` URL, and no script-only activation handler as the sole means by which the Back_To_Top_Control functions.
7. THE Site SHALL give the Back_To_Top_Control an accessible name that conveys its purpose, provided by its visible text content or by an `aria-label`, and SHALL render visible text content for the control on all nine Content_Pages.
8. THE Site SHALL make the Back_To_Top_Control reachable by sequential keyboard navigation on all nine Content_Pages without declaring a positive `tabindex` value on it or on any ancestor.
9. WHILE the Back_To_Top_Control holds keyboard focus, THE Site SHALL render a focus indicator that is at least 2 CSS pixels thick, spans at least the full width of the control's text, and has a Contrast_Ratio of at least 3.0:1 against the Copyright_Block background `#1e252d`.
10. THE Site SHALL render a visible Design_Credit inside the Copyright_Block on all nine Content_Pages, naming HTML5 UP and containing an `<a>` element whose `href` is `https://html5up.net`.
11. THE Site SHALL word the Design_Credit as a credit for the template or site design, and SHALL NOT word it as a credit for fonts or icons, because Heading_Font and Body_Font are supplied by Alberto Fontense and Pangram Pangram Foundry and are credited under Requirement 9 criterion 3.
12. THE Site SHALL render the Design_Credit and the Back_To_Top_Control as two separate elements, such that activating the Back_To_Top_Control performs the criterion 2 outcome and follows no external link, and activating the Design_Credit link navigates to `https://html5up.net`.
13. THE Site SHALL emit identical Copyright_Block inner markup structure, identical Back_To_Top_Control text, and identical Design_Credit text on all nine Content_Pages, including on `killerbyte.html`, `launchtoy.html`, and `vexlego.html` where the `#copyright` div is written on a single source line.
14. THE Site SHALL emit well-formed HTML in the replaced Copyright_Block markup on all nine Content_Pages, escaping every literal `&` character as `&amp;`.
15. THE Site SHALL preserve the Copyright_Block Chrome_Text typography — the Body_Font family, the 0.8rem `font-size`, the uppercase transform, the declared `letter-spacing`, the 1.5 `line-height`, and the centred text alignment — and SHALL change the Copyright_Block colour only as Requirement 14 requires.
16. THE Site SHALL render the Back_To_Top_Control and the Design_Credit wholly within the Copyright_Block bounds with no clipped character and no horizontal page scrollbar, at viewport widths of 320px, 768px, 1024px, and 1440px.
17. WHILE the pointer is over the Back_To_Top_Control or over the Design_Credit link, THE Site SHALL render a pointer cursor, overriding the `cursor: default` declaration that the Copyright_Block applies to its static text.

### Requirement 14: Legible Copyright Bar

**User Story:** As a visitor using the footer, I want the copyright bar readable, so that I can find and use the Back to top control rather than guess at a faint line of text.

The Copyright_Block is currently white at alpha 0.25 over `#1e252d`, which composites to a measured Contrast_Ratio of **2.27:1** — far below the 4.5:1 that Requirement 5 criterion 6 requires of Chrome_Text. Conflict C3 in the design document resolved that shortfall by leaving the colour unchanged, on the ground that Requirement 1 criterion 11 pinned it. **The owner has now reversed that ruling for this element**, because the Copyright_Block hosts an interactive control under Requirement 13 rather than static text only. This requirement therefore **resolves conflict C3** in the opposite direction, and Requirement 1 criterion 11 has been amended to exempt the Copyright_Block. The reversal is scoped to the Copyright_Block: the footer `h3` shortfall recorded as conflict C2 is untouched.

#### Acceptance Criteria

1. THE Site SHALL render the Copyright_Block text at a Contrast_Ratio of at least 4.5:1 against the Copyright_Block background `#1e252d` on all nine Content_Pages.
2. THE Site SHALL render the Back_To_Top_Control text and the Design_Credit link text at a Contrast_Ratio of at least 4.5:1 against the Copyright_Block background `#1e252d` in their default states on all nine Content_Pages.
3. WHILE the Back_To_Top_Control or the Design_Credit link is hovered, focused, or active, THE Site SHALL render its text at a Contrast_Ratio of at least 4.5:1 against the Copyright_Block background `#1e252d`.
4. THE Site SHALL achieve criteria 1 through 3 by raising the opacity of the existing white Copyright_Block text colour over `#1e252d`, and SHALL in satisfying this requirement leave the opaque white `#ffffff`, the Copyright_Block background colour, and every other colour value in the Compiled_Stylesheet unchanged, so that Requirement 8 criterion 7 continues to hold with the Footer_Email_Link colour as the single changed value.
5. THE Site SHALL declare a single Copyright_Block text colour value that resolves identically on all nine Content_Pages, and SHALL apply the change to the `#copyright` rule only, leaving every other rule that declares a colour over the dark footer background unchanged.
6. WHEN the design phase fixes the Copyright_Block opacity value, THE Site SHALL record in the design document the composited hexadecimal colour and the measured Contrast_Ratio against `#1e252d` for the chosen value, so that criterion 1 is established by measurement rather than by a carried-forward estimate. The remedy documented before this amendment — raising the alpha to approximately 0.65 — is directionally correct but its paired ratio of approximately 4.6:1 does not reconcile with the composite: white at alpha 0.65 over `#1e252d` composites to approximately `#b0b3b6` and measures approximately **7.3:1**, whereas approximately 4.6:1 is reached at an alpha of approximately **0.49**. Either value satisfies criterion 1; the design phase SHALL measure and record the one it declares rather than restate the unreconciled pairing.
7. WHEN the design phase updates Correctness Property 1's accepted-exceptions set, THE Site SHALL remove the `#copyright` entry recorded at 2.27:1 from that set and SHALL retain the footer `h3` entry recorded at 4.05:1 as the only remaining accepted exception, because the `#copyright` shortfall is now fixed rather than accepted and an accepted-exception entry whose measured ratio no longer matches its recorded value would otherwise fail the property.
8. THE Site SHALL leave the footer `h3` label colour and the footer social icon link colours unchanged, as Requirement 1 criterion 11 continues to require, so that conflict C2 remains resolved as previously decided.
9. THE Site SHALL declare the Copyright_Block colour change in the Compiled_Stylesheet, and SHALL introduce no inline `style` attribute and no in-page `<style>` block on any Content_Page to achieve it.

### Requirement 15: Centred Copyright Divider

**User Story:** As a visitor looking at the footer, I want the divider bar between the two copyright items to sit on the centre line of the bar, so that the footer reads as a deliberately balanced pair rather than as a rule that has drifted off centre.

The Copyright_Divider is measurably off centre. At a viewport width of 1440px the Copyright_Row content box spans x 144 to x 1296, putting its horizontal centre at **x 720**, while the Copyright_Divider renders at **x 699.9** — **20.1px to the left of centre**.

The cause is structural, not a stray value. Both Copyright_Item elements are `display: inline-block` inside a block that declares `text-align: center`, so what is centred is the **combined text run** of the two items. The boundary between them therefore falls wherever their unequal widths place it, and the two labels are not equal: "Back to top" is 11 characters and "Design: HTML5 UP" is 16. Centring the run puts the join left of the box centre by roughly half the width difference. No adjustment to margin, padding, or letter-spacing fixes that, because the offset is a function of the label widths — it would reappear, at a different magnitude, the moment either label's text changed length. The requirement below is therefore written against the divider's own position, not against the run's, and criterion 4 makes label-width independence explicit.

The Stacked_Layout is untouched. At the `<=xsmall` breakpoint the items become full-width blocks and `border-left: 0` removes the divider entirely, so there is no divider to centre at 320px. Criteria 1 through 4 are scoped to the Side_By_Side_Layout for that reason, and criterion 6 states the scope so that this requirement is not read as demanding a divider at narrow widths where none exists.

#### Acceptance Criteria

1. WHILE the Copyright_Row is in the Side_By_Side_Layout, THE Site SHALL render the horizontal centre of the Copyright_Divider box within 1 CSS pixel of the horizontal centre of the Copyright_Row content box, on all nine Content_Pages.
2. THE Site SHALL satisfy criterion 1 at each of the viewport widths 768px, 1024px, and 1440px, these being the widths used elsewhere in this document at which the Side_By_Side_Layout applies.
3. WHILE the Copyright_Row is in the Side_By_Side_Layout, THE Site SHALL render the horizontal centre of the Copyright_Row content box within 1 CSS pixel of the horizontal centre of the Copyright_Block content box, at each of the viewport widths named in criterion 2, so that the centre line criterion 1 targets is also the Copyright_Block's own centre line.
4. THE Site SHALL satisfy criterion 1 for any pair of Copyright_Item label strings, including a pair whose rendered character counts differ by at least 8 and a pair whose rendered character counts are equal, so that the Copyright_Divider stays centred when either label's text changes length.
5. WHILE the Copyright_Row is in the Stacked_Layout, THE Site SHALL render each Copyright_Item as a full-width block box on its own line with the `border-left` removed, preserving the `<=xsmall` treatment declared in the Compiled_Stylesheet's `max-width: 480px` block — `display: block`, `border-left: 0`, `margin: 1rem 0 0 0`, `padding-left: 0`, and the first item's top margin reset to zero.
6. THE Site SHALL apply criteria 1 through 4 only while the Copyright_Row is in the Side_By_Side_Layout, because the Stacked_Layout renders no Copyright_Divider; at a viewport width of 320px the Stacked_Layout applies and criterion 5 governs instead.
7. THE Site SHALL render both Copyright_Item labels — "Back to top" and "Design: HTML5 UP" — with every character fully visible inside the Copyright_Row content box at each of the viewport widths 320px, 768px, 1024px, and 1440px, with no character clipped by that box, no ellipsis or other truncation indicator applied, and no horizontal page scrollbar produced.
8. WHILE the Copyright_Row is in the Side_By_Side_Layout, THE Site SHALL render each Copyright_Item label clear of the Copyright_Divider, such that no rendered glyph of either label overlaps the Copyright_Divider box and the nearest rendered glyph edge of each label stands at least 8 CSS pixels from the nearer Copyright_Divider edge, at each of the viewport widths named in criterion 2.
9. THE Site SHALL render the Copyright_Divider at a width of 2 CSS pixels and SHALL resolve its colour to the Copyright_Block `color` value that Requirement 14 sets, leaving the declared width and the inherited colour unchanged from their pre-amendment values.
10. THE Site SHALL preserve the Copyright_Block typography listed in Requirement 13 criterion 15 — the Body_Font family, the 0.8rem `font-size`, the uppercase transform, the declared `letter-spacing`, the 1.5 `line-height`, and the centred text alignment — together with the Copyright_Block `margin`, `width`, `max-width`, and `<=large` margin override, so that centring the Copyright_Divider changes no other footer geometry and no footer element's height.
11. THE Site SHALL preserve the Back_To_Top_Control behaviour required by Requirement 13 criteria 2 through 6, the Design_Credit link behaviour required by Requirement 13 criteria 10 through 12, the focus indicator required by Requirement 13 criterion 9, and the pointer cursor required by Requirement 13 criterion 17.
12. THE Site SHALL preserve the Copyright_Block inner markup required by Requirement 13 criteria 13 and 14 — the same element set, count, order, and nesting, byte-identical inner markup across all nine Content_Pages — and SHALL achieve the centring through the Compiled_Stylesheet, adding, removing, and reordering no element inside the Copyright_Block.
13. THE Site SHALL declare the centring in the Compiled_Stylesheet, and SHALL introduce no inline `style` attribute and no in-page `<style>` block on any Content_Page to achieve it.
14. WHEN the design phase fixes the Copyright_Row layout, THE Site SHALL record in the design document, for each of the viewport widths named in criterion 2, the measured Copyright_Row content-box left and right x-coordinates, the measured Copyright_Divider centre x-coordinate, and the resulting signed offset from the Copyright_Row centre, and SHALL record the same three measurements taken again after substituting a label pair whose rendered character counts differ by at least 8, so that criteria 1 and 4 are established by measurement rather than asserted.

### Requirement 16: Bold Weight for the Nav Panel Toggle and Nav Panel Links

**User Story:** As the site owner, I want the mobile Menu control and the links inside the slide-out panel set in Telegraf Ultrabold, so that the mobile navigation chrome reads as heavily as the desktop navigation it stands in for.

Two declaration sites in the Compiled_Stylesheet move from the body weight (400) to the bold weight (800): the `#navPanelToggle` rule and the `#navPanel .links li a` rule. The Body_Font stack is unchanged at both sites, as are both declared `font-size` values.

This closes an inconsistency that Change Set 2 left open. The Nav_Panel_Link elements are not a separate set of anchors — `assets/js/main.js` moves the children of `#nav` into `#navPanel > nav` at the `<=medium` breakpoint and back above it, so the same two anchors, "Projects" and "CAD Gallery", rendered at 800 while parented to `#nav` and at 400 once reparented to `#navPanel`. One pair of links changed weight when the viewport crossed 980px. After this amendment they carry one weight throughout.

The 800 face already ships as `assets/webfonts/PPTelegraf-Ultrabold.otf` and is already declared at `font-weight: 800` under the `PP Telegraf` CSS family, so no font file is added and the Requirement 2 bundle budget is untouched. Ultrabold measures between **3.6% and 8.5% wider** than Regular, which matters most for the Nav_Panel_Toggle: it is `position: fixed` with `right: 0.75rem` and an automatic width, so the heavier label grows the box leftward, toward the header title. Criteria 9 through 13 bound that.

Both elements render `display: none` above the `<=medium` breakpoint, so the layout criteria name only 320px and 768px. The weight and family criteria are declaration-level and hold at all four widths regardless.

#### Acceptance Criteria

1. THE Site SHALL render the Nav_Panel_Toggle at a computed `font-weight` of 800 on all nine Content_Pages.
2. THE Site SHALL render every Nav_Panel_Link at a computed `font-weight` of 800 on all nine Content_Pages.
3. **Deleted in Change Set 4**, for the same reason as Requirement 11 criterion 2: it named `_font(weight-bold)`, two `assets/sass/layout/_navPanel.scss` line numbers and the absence of per-rule literals, all of which describe a mechanism that no longer exists and none of which describes anything observable. Criteria 1, 2 and 7 carry the whole of the substance — computed weight 800 at both sites on all nine pages, matching a shipped face. The criterion number is retained rather than reused so that references elsewhere stay valid.
4. THE Site SHALL resolve the computed `font-family` of the Nav_Panel_Toggle and of every Nav_Panel_Link to a stack whose first family is Body_Font, unchanged from its pre-amendment value.
5. THE Site SHALL leave the declared `font-size` of the Nav_Panel_Toggle at 0.9rem, and at 0.8rem at the `<=small` breakpoint, and the declared `font-size` of every Nav_Panel_Link at 0.9rem, each unchanged from its pre-amendment value.
6. THE Site SHALL serve the 800 weight from the `PPTelegraf-Ultrabold.otf` face already present in the Webfont_Bundle, and SHALL add no font file, remove no font file, and change no `@font-face` rule in satisfying this requirement.
7. THE Site SHALL render the Nav_Panel_Toggle and every Nav_Panel_Link at a computed `font-weight` equal to the `usWeightClass` of a face present in the Webfont_Bundle, and SHALL permit no browser-synthesized, emboldened, or interpolated weight for either element, satisfying Requirement 5 criterion 2.
8. THE Site SHALL satisfy criteria 1 through 7 as declared and computed values at each of the viewport widths 320px, 768px, 1024px, and 1440px, notwithstanding that the Nav_Panel_Toggle and the nav panel render `display: none` above the `<=medium` breakpoint.
9. WHILE the viewport width matches the `<=medium` breakpoint, THE Site SHALL render the Nav_Panel_Toggle label "Menu" together with its `\f0c9` icon glyph on a single line wholly inside the Nav_Panel_Toggle padding box, with no character clipped by that box, no ellipsis or other truncation indicator applied, and no horizontal page scrollbar produced, at viewport widths of 320px and 768px.
10. WHILE the viewport width matches the `<=medium` breakpoint, THE Site SHALL render the Nav_Panel_Toggle wholly inside the viewport with its right border-box edge 0.75rem from the viewport's right edge and its top border-box edge 0.75rem from the viewport's top edge, so that the wider label extends the box leftward within its fixed position rather than moving or overflowing it, at viewport widths of 320px and 768px.
11. WHILE the viewport width matches the `<=medium` breakpoint, THE Site SHALL render the Nav_Panel_Toggle box clear of the `#header` title, with no rendered glyph of the Nav_Panel_Toggle overlapping any rendered glyph of the `#header` title, at viewport widths of 320px and 768px.
12. WHILE the nav panel is visible, THE Site SHALL render every Nav_Panel_Link label — "Projects" and "CAD Gallery" — on a single line wholly inside the `#navPanel` content box, with no character clipped by that box and no Nav_Panel_Link overlapping another Nav_Panel_Link or the `#navPanel .close` control, at viewport widths of 320px and 768px.
13. IF the Nav_Panel_Toggle label or a Nav_Panel_Link label cannot satisfy criteria 9 through 12 at its declared `font-size` and its declared `letter-spacing`, THEN THE Site SHALL enlarge the containing box or reduce the element's horizontal padding, and SHALL NOT reduce the declared `font-size` below the floor set by Requirement 5 criterion 3, SHALL NOT return either element to the body weight, and SHALL NOT apply `text-overflow` truncation.
14. THE Site SHALL declare the `letter-spacing` of the Nav_Panel_Toggle and of every Nav_Panel_Link at 0.05em, which falls within the 0.025em to 0.075em range required by Requirement 5 criterion 8, and SHALL declare no value smaller than 0.05em for either element, because the heavier face reduces the white space between glyphs at a constant tracking value.
15. THE Site SHALL preserve for the Nav_Panel_Toggle and for every Nav_Panel_Link its pre-amendment `text-transform`, default and hover `color`, `background-color`, `border`, `box-shadow`, `padding`, and transition timing, including the `#navPanelToggle.alt` scrolled-state background, box shadow, colour, and hover background.
16. THE Site SHALL render the Nav_Panel_Toggle and every Nav_Panel_Link at a Contrast_Ratio of at least 4.5:1 against the background of its containing block in its default state and in its hover state, in both the plain Nav_Panel_Toggle state and the `.alt` scrolled state, satisfying Requirement 5 criterion 6 at the heavier weight.
17. THE Site SHALL render the top navigation links and the Nav_Panel_Link elements at the same computed `font-weight` of 800, so that the anchors `assets/js/main.js` moves between `#nav` and `#navPanel` at the `<=medium` breakpoint carry one weight on both sides of that move.
18. THE Site SHALL render the Nav_Panel_Toggle and every Nav_Panel_Link at the declared `font-size` values named in criterion 5 with no two adjacent glyph outlines overlapping or touching and with every enclosed counter shape remaining open rather than filled, at viewport widths of 320px and 768px.
19. THE Site SHALL declare both weight changes in the Compiled_Stylesheet, at the `#navPanelToggle` rule and the `#navPanel .links li a` rule, and SHALL introduce no inline `style` attribute and no in-page `<style>` block on any Content_Page to achieve them.
20. THE Site SHALL preserve the nav panel behaviour defined in `assets/js/main.js` — the toggle opening and closing the panel, the `<=medium` and `>medium` reparenting of the `#nav` children, and the `.alt` class applied and removed on scroll — with no uncaught script error reported by the browser, satisfying Requirement 8 criterion 6.
21. WHEN the design phase fixes the Nav_Panel_Toggle and Nav_Panel_Link declarations, THE Site SHALL record in the design document, for the Nav_Panel_Toggle label and for the widest Nav_Panel_Link label, the measured rendered label width at the body weight and at the bold weight at viewport widths of 320px and 768px, together with the resulting Nav_Panel_Toggle border-box width and the x-coordinate of its left border-box edge, so that the advance-width increase and the criterion 11 clearance are established by measurement rather than assumed.

### Requirement 17: Short README with Retained Attributions and a Linked Sync Document

**User Story:** As the site owner, I want `README.md` back to the short credits file it was before this refresh, so that a visitor to the repository sees what the site is and who is credited without reading a maintenance manual.

`README.md` is currently 134 lines. Change Set 1 and Change Set 2 added a typography-notes section, a weights table, an italic-face note, a seven-step stylesheet regeneration procedure, and several parity caveats. The owner wants the file back to roughly its pre-Change-Set-1 length and shape: a title, a one-line link to the deployed site, and a Credits section.

Two obligations currently force content into `README.md`, and both are amended rather than dropped:

- **Requirement 9 criterion 3** requires Horizon and Telegraf to be credited with their designers and licence tiers in the Readme_Credits section. **That obligation is kept.** It fits the short structure as one more `**Fonts:**` bullet, and criterion 3 is amended only to permit that compact form. The template credit in the same section independently satisfies the HTML5 UP Creative Commons Attribution 3.0 condition recorded in Requirement 13, so it stays as well.
- **Requirement 7 criterion 6** (criterion 5 before the Change Set 4 rewrite) requires the stylesheet documentation to be written down, naming the file each item concerns and how each is verified. That content is long, and it is the main reason the README grew. **Its location moves to the Sync_Document.** `README.md` links the Sync_Document in one line. Requirement 7 criteria 7 through 9 pin that, and criterion 9 makes deletion a defect. **Amended in Change Set 4:** what the Sync_Document holds is no longer a regeneration-and-parity procedure — the SASS tree it regenerated from is gone — but the five-item maintenance note that Requirement 7 criterion 8 now enumerates. The relocation obligation is unchanged; what is relocated is shorter, because the parity half of it described work that no longer exists.

Nothing removed from the README is discarded. The stylesheet notes and their caveats move to the Sync_Document; the weights table, the no-italic-face note, and the non-commercial standing-obligation statement move to the Sync_Document or the Provenance_Record. The Pages workflow uploads `path: '.'` and prunes only `tools` and `.kiro`, so the new `docs/` directory would otherwise be published; criterion 11 adds it to that prune step, following the precedent the workflow already sets for non-site files.

#### Acceptance Criteria

1. THE Site SHALL provide `README.md` at the repository root containing a level-1 heading naming the site, a single line of body text linking the deployed site at `https://jefferyxr.github.io/personal-website/index.html`, and a `## Credits` section.
2. THE Site SHALL keep `README.md` at 40 lines or fewer, reducing it from the 134 lines it carries before this amendment to approximately its pre-Change-Set-1 length.
3. THE Readme_Credits section SHALL contain a template credit naming Massively and HTML5 UP with a Markdown link to `https://html5up.net`, an icons credit naming Font Awesome, a libraries credit naming jQuery, Scrollex, and Responsive Tools, and a fonts credit naming Horizon with Alberto Fontense and its free personal-use tier and Telegraf with Pangram Pangram Foundry and its free personal, non-commercial tier.
4. THE Site SHALL satisfy Requirement 9 criterion 3 through the fonts credit required by criterion 3, expressed as a single bullet of four lines or fewer.
5. THE Site SHALL retain the template credit and the fonts credit required by criterion 3, these being the attribution conditions of the Creative Commons Attribution 3.0 grant recorded in Requirement 13 and of the two font licence tiers recorded in Requirement 9, and SHALL retain them independently of any reduction in the length of `README.md`.
6. IF an attribution named in criterion 3 is absent from `README.md`, THEN THE Site SHALL be treated as failing this requirement, and the omission SHALL be reported as a defect naming the missing attribution.
7. THE Site SHALL link the Sync_Document from `README.md` in a single line of body text, satisfying Requirement 7 criterion 7.
8. THE Site SHALL provide the Sync_Document at `docs/stylesheet-sync.md`, holding the maintenance note required by Requirement 7 criterion 6 with all five items, every file name, and every verification instruction listed in Requirement 7 criterion 8, and SHALL state in it that `assets/css/main.css` is the stylesheet source with no SASS tree and no compiler behind it. **Amended in Change Set 4:** the five items replace the seven-step regeneration procedure this criterion previously required, which described a mirroring task that no longer exists.
9. THE Site SHALL relocate, rather than delete, each statement removed from `README.md` — the stylesheet maintenance items and their caveats, the declared-weights table, the no-italic-face note, and the non-commercial standing-obligation statement — into the Sync_Document or the Provenance_Record, such that each relocated statement is present in at least one repository document. The parity-verification steps are **exempt from this criterion in Change Set 4**: they are not relocated but retired, because the second artifact they compared against has been deleted.
10. THE Site SHALL keep a reference to the Provenance_Record at `assets/webfonts/FONT-PROVENANCE.md` in `README.md` or in the Sync_Document, and SHALL keep the Provenance_Record itself present and complete as Requirement 9 requires.
11. THE Site SHALL name `docs` in the prune step of `.github/workflows/static.yml` alongside the existing `tools` and `.kiro` entries, so that the Sync_Document is excluded from the deployed Pages artifact, because that workflow uploads `path: '.'` and publishes everything the checkout retains.
12. THE Site SHALL resolve every Markdown link in `README.md` to an existing target, with each relative link resolving to a file present in the repository and the Sync_Document link resolving to `docs/stylesheet-sync.md`.
13. THE Site SHALL leave the nine Content_Pages, the Compiled_Stylesheet, the Noscript_Stylesheet, the Webfont_Bundle, and the Provenance_Record unchanged in satisfying this requirement, restricting the change to `README.md`, the Sync_Document, and the prune step named in criterion 11.

## Assumptions and Open Questions

1. **RESOLVED — Body_Font delivery format (Requirement 2).** The free Telegraf tier ships desktop formats only; WOFF and WOFF2 are paid-licence deliverables, and the foundry's terms restrict modifying the font file. Of the four options considered — (a) request written permission to convert, (b) purchase a Web licence for WOFF2, (c) serve the vendor-supplied OTF/TTF, (d) substitute another body typeface — **option (c) was chosen**: Body_Font is served as the vendor-supplied OTF or TTF file through `@font-face`, with no conversion, no purchase, and no font substitution, under a 600 KB Webfont_Bundle budget rather than the 250 KB budget an all-WOFF2 bundle would have allowed. The accepted trade-off is a materially larger font payload than WOFF2 would give. Requirement 2 criteria 3, 6, 12, 13, 16, 17, and 18 encode this decision, and Requirement 9 criterion 6 now depends on it.
2. **RESOLVED — Horizon has no bold weight (Requirement 3).** Horizon's free release provides a single solid face; Outline, Outline Two, Lines and Lines Two are decorative styles, not weights. Change Set 1 measured that face at `usWeightClass` **700** and declared `weight-heading: 700` against it, so no synthesized bold occurs. The heading hierarchy is carried by size alone and no decorative style is used at any heading level. Requirement 3 criterion 4 is satisfied as written.
3. **RESOLVED — `h1` sizing (Requirement 3).** Horizon proved narrower than the pessimistic estimate: `JEFFERY ROSS` measures 10.999em effective at −0.01em tracking, and 768px is the binding viewport. Change Set 1 declares the intro `h1` at **3.75rem** (7.72% margin at 768px), **2.75rem** at `<=small`, the project-page `h1` at **2.5rem**, and **2rem** at `<=small`. 4rem does fit at 768px but was rejected on a 1.03% margin. The values are browser-confirmed with the real faces loaded, so Requirement 3 criterion 6 no longer needs empirical work.
4. **Provenance (Requirement 9).** Both fonts must be downloaded from the designer's or foundry's own channels — Creative Market / VP Creative Shop for Horizon, [pangrampangram.com](https://pangrampangram.com/products/telegraf/) for Telegraf. The aggregator link supplied during requirements gathering, [fontdownloader.net](https://fontdownloader.net/horizon-font/), informed the format and glyph-coverage facts recorded here but must not be the download source.
5. **Non-commercial status is a standing obligation, not a one-time check (Requirement 9).** Both licences remain valid only while the Site stays a personal job-application showcase. Adding freelance solicitation, rates, or any monetisation later would breach both licences, so this constraint must survive future content changes.
6. **RESOLVED — which Telegraf styles the free tier includes (Requirement 4).** The free download supplied nine OTFs. A true bold face exists, so Requirement 4 criterion 4's no-bold branch does not apply and no README limitation note is required. **`weight` is 400 and `weight-bold` is 800**, not 700: there is no 700 face in the download, and Requirement 4 criterion 3 requires every declared weight to match a shipped face. Two faces ship — `PPTelegraf-Regular.otf` (400) and `PPTelegraf-Ultrabold.otf` (800) — totalling 86 KB of the 600 KB budget, with the seven unshipped styles removed under Requirement 2 criterion 16. No italic face ships, because a content audit found 15 `<strong>` elements and zero `<em>` and `<i>` elements across the nine pages. Requirement 11 now takes the same 800 face for Bold_Chrome_Text, so that requirement adds no file and does not reopen the budget question.
7. **RESOLVED — Footer_Email_Link colour (Requirement 1).** The default-state colour is **`#3a4148`**, which measures a Contrast_Ratio of **9.49:1** against Footer_Background `#f5f5f5` and so clears the 7.0:1 WCAG AAA threshold retained as the target in Requirement 1 criterion 1. Change Set 1 shipped the lighter candidate `#4a5158` (7.38:1); Change Set 2 supersedes it with the darker value the owner has now chosen. 9.49:1 is the computed value and supersedes the 9.5:1 figure cited in earlier drafts of this document — the discrepancy was rounding, not a different measurement. The threshold in criterion 1 is unchanged. Requirement 1 criterion 12 declares the literal value, criterion 13 pins it to the `alt.fg-link` token, criterion 7 ties the underline to it, and Requirement 8 criterion 7 names it as the single permitted palette change.
8. **RESOLVED in Change Set 4 — source of truth (Requirement 7).** This item recorded the position that the repository has no `package.json` and no SASS compiler, that `assets/css/main.css` is hand-edited alongside the SASS files, and that Requirement 7 would keep the two in sync with a documented procedure rather than introduce a build step. **The SASS tree is now deleted** (item 16), so there is one artifact and nothing to keep in sync: `assets/css/main.css` is the source of truth by construction rather than by discipline, and the parity-maintenance burden this item created is retired. The no-build-step decision stands, and is now unconditional.
9. **ACCEPTED POSITION — no Horizon licence text file exists (Requirement 9).** No licence or EULA text for Horizon could be located from the designer's channels. This is **not an open TODO**: Requirement 9 criterion 2 no longer asks for a stored text file for Heading_Font, criterion 9 substitutes a recorded-fields obligation (licence tier, designer, source URL, download date) in the Provenance_Record, and criterion 11 requires the `TODO (owner)` note under the `Horizon.woff2` record in `assets/webfonts/FONT-PROVENANCE.md` to be replaced by a statement of this accepted position. The design and tasks phases handle the file edit. **The substantive obligation is unchanged** — criterion 10 states plainly that the free-personal-use terms bind the Site whether or not a copy of the text is stored, so Requirement 9's Non_Commercial_Use constraint and its criteria 4 and 5 apply to Horizon exactly as before. Nothing here weakens the licence; it only stops the spec asking for a document that does not exist.
10. **RESOLVED — `#copyright` legibility, reversing conflict C3 (Requirement 14).** The owner has decided the copyright bar must be legible, overturning the earlier "leave unchanged" resolution of conflict C3 for this element. The reason is that the bar now hosts the Back_To_Top_Control, so it carries an interactive target rather than static text. Requirement 1 criterion 11 is amended to exempt the Copyright_Block and nothing else; the footer `h3` (conflict C2) and the social icon links remain pinned. Two consequences to carry into design: the accepted-exceptions set of Correctness Property 1 must **drop** the `#copyright` 2.27:1 entry, leaving the footer `h3` 4.05:1 entry alone (Requirement 14 criterion 7); and the previously documented remedy needs re-measuring, because "alpha ~0.65 → ≈4.6:1" does not reconcile — white at alpha 0.65 over `#1e252d` composites to roughly `#b0b3b6` at about **7.3:1**, while about **4.6:1** falls at an alpha near **0.49**. Both clear the 4.5:1 bar, so the choice is a design decision, but the recorded pairing must be corrected rather than carried forward (Requirement 14 criterion 6).
11. **DECIDED — the HTML5 UP attribution stays (Requirement 13).** The owner asked whether the Massively credit could be dropped, given that the fonts are no longer HTML5 UP's and the site now looks and behaves very differently from the published demo. The answer recorded here is **no**. The [HTML5 UP licence](https://html5up.net/license) grants use and modification under Creative Commons Attribution 3.0 with the credit named as the consideration in exchange, and HTML5 UP sells attribution-free usage separately through Pixelarity — so the credit is the price of the free tier, not a courtesy. CC BY 3.0 attaches attribution to derivative works as well as verbatim copies, so extensive divergence does not discharge the condition. The codebase also remains substantially template-derived: 22 SASS partials carry the Massively header (plus `main.scss` and `noscript.scss`), six template JavaScript files are still shipped, and the `#wrapper`, `is-preload`, `split contact`, `icons`, and `actions` structures are on all nine pages. What does change is the wording — "Fonts & icons: HTML5 UP" is now inaccurate, so Requirement 13 criteria 10 and 11 require a design credit instead. If the owner wants the credit gone, the supported route is a Pixelarity licence, which would be a new decision outside this spec.
12. **Front-loaded risk in Change Set 2 (Requirements 11 and 12).** The bold-weight change and the pill-sizing correction are coupled: Ultrabold widens every label at an unchanged font size, and the 0.55rem homepage pills are the tightest case on the Site. Requirements 11 and 12 both defer exact values to measurement (Requirement 11 criterion 16, Requirement 12 criterion 13) rather than pinning pixels here. Note the pre-existing tension that Change Set 2 does not resolve: Requirement 5 criterion 3 sets a 0.7rem computed floor for Chrome_Text, while the homepage pills are declared at 0.55rem. Requirement 11 criterion 12 and Requirement 12 criterion 11 both forbid *lowering* the declared size further, but neither raises it, so that shortfall stands as it did after Change Set 1 and is out of scope for this amendment.

13. **OPEN — the mechanism for centring the Copyright_Divider (Requirement 15).** The offset is measured and its cause is understood: at 1440px the Copyright_Row spans x 144→1296 (centre x 720) while the divider renders at x 699.9, 20.1px left of centre, because two `inline-block` items inside a `text-align: center` block centre their *combined run* and the join between them lands wherever the unequal label widths put it. The **mechanism is not chosen here.** Requirement 15 states the position the divider must hold and leaves the means to design. Candidates worth measuring: giving the Copyright_Row `display: flex` with `justify-content: center` and each item an equal `flex-basis: 50%`, so the items' shared edge is the box centre; a three-column `display: grid` of `1fr auto 1fr` with the divider as the middle track; absolutely positioning the divider at `left: 50%` against a `position: relative` Copyright_Row; or keeping the inline-block flow and equalising the two items' widths. Each interacts differently with the `<=xsmall` Stacked_Layout, which criterion 5 requires intact, and with the 1rem gaps criterion 8 depends on. Requirement 15 criterion 14 requires the chosen mechanism's measurements — including a re-measurement after substituting a label pair whose lengths differ by at least 8 characters — to be recorded, because criterion 4's label-width independence is the whole point of the change and a mechanism that merely happens to centre the current two strings would pass a single-case check and fail the requirement.
14. **RESOLVED — the nav panel links and the top nav links are the same anchors (Requirement 16).** `assets/js/main.js` appends the children of `#nav` into `#navPanel > nav` at the `<=medium` breakpoint and returns them above it, so "Projects" and "CAD Gallery" are one pair of `a` elements that changes parent. Change Set 2 set `#nav ul.links a` to 800 and left `#navPanel .links li a` at 400, which meant those two anchors changed weight as the viewport crossed 980px. Requirement 16 criterion 17 closes that. Two incidental notes for the implementation phase: the `#navPanel .links li a` rule declares `font-size: 0.9rem` **twice**, in the Compiled_Stylesheet — a pre-existing duplicate that is harmless, is left as it is, and is why Requirement 7 criterion 8 keeps the last-declaration-wins item (which, since Change Set 4, warns a *reader* which declaration paints rather than warning a parity checker which one to compare); and the Nav_Panel_Toggle text is injected by `assets/js/main.js` rather than authored in any of the nine pages, so no Content_Page markup changes.
15. **DECIDED, then narrowed by Change Set 4 — the stylesheet documentation moves, and the parity half of it is retired (Requirements 7 and 17).** The owner asked for a short README, and the seven-step regeneration procedure was the bulk of what made it long. Change Set 3 recorded the answer as **relocate, do not condense**: move it to `docs/stylesheet-sync.md` and link it from the README in one line. **Change Set 4 changes what is relocated, not the decision to relocate it.** With the SASS tree deleted there is no regeneration and no parity to verify, so the steps that described mirroring an edit from one artifact into another are **retired rather than moved** — Requirement 17 criterion 9 says so explicitly, so their absence is not read as content lost. The items that survive are the ones that were never about parity in the first place: the `@import`/`@font-face` ordering item (an `@font-face` above line 1 silently kills the Font Awesome import and every icon on all nine pages), the zero-occurrence items, the smooth-scroll guard, the last-declaration-wins item (`#footer` and `#copyright` each carry `color` twice — and for `#copyright` the first value is the opaque `#ffffff`, not the value that renders, so a *reader* who trusts the first declaration computes a contrast ratio for a colour that is never painted), and the per-page Copyright_Block markup item. Requirement 7 criterion 8 enumerates the five and criterion 9 makes any omission a reportable defect.
16. **RESOLVED — the SASS tree is deleted; `assets/css/main.css` is the sole stylesheet source (Requirement 7, and the sweep through Requirements 1, 3, 4, 5, 8, 10, 11, 12, 15, 16 and 17).** `assets/sass/` — 31 files — has been removed from the repository.

    **Why.** Three facts, each sufficient on its own. It was **never compiled**: there is no `package.json`, no compiler, no build step, no source map, and no Content_Page or stylesheet references it, so nothing in the deployed site ever depended on it. It had **drifted past the point of being able to regenerate the site**: compiling it would have produced a `main.css` missing several rules that only ever existed in the compiled file, so the tree was not a source of the shipped stylesheet in any usable sense — it was a stale parallel description of it. And it was a **second source of truth that had already produced live divergences**, which is the failure mode risk R4 named and Requirement 7's parity criteria existed to detect. A checkable-but-unfixable divergence is worse than no second copy, because it costs verification effort on every change set and pays nothing back.

    **What was considered and rejected: introducing a real build step.** That is the alternative that would have kept the tree, and it was rejected because it cannot be done cheaply: every compiled-only rule would have to be **back-ported into the SASS first**, one at a time, with each back-port verified against the rendered page — and the compiled-only set is not small or fully enumerated. Adding a compiler to a tree that does not reproduce the site converts a documentation problem into a rendering regression. Reversing the direction — generate the SASS from the CSS — produces a file nobody edits, which is the same dead artifact under a different name. Deleting the stale copy is the only option whose cost is bounded and whose risk is zero at deploy time.

    **What the deletion settles.** Requirement 7's parity criteria (old c1, c3, c4, c8) are deleted rather than restated; item 8 above is resolved, and with it the **parity-maintenance burden** that every previous change set paid — the SASS-first-then-mirror sequencing rule in `tasks.md`, the "declare it in both artifacts" clause that Requirements 10, 14, 15 and 16 each carried, and Correctness Property 2 in `design.md`. Risk R4 is retired by construction. What it does *not* settle is recorded as the residual risk in `design.md`: `main.css` is now hand-edited with no source structure — no variables, no nesting, no single place to change a family or a weight — and what stands in for that structure is the maintenance note plus the property suite. That trade is deliberate and it is the reason Requirement 7 criteria 6 through 9 survive the rewrite intact.

17. **NOTED — `docs/` would be published unless pruned (Requirement 17).** `.github/workflows/static.yml` uploads `path: '.'` and prunes only `tools` and `.kiro` from the ephemeral CI checkout. A new `docs/` directory therefore ships to GitHub Pages by default. Requirement 17 criterion 11 adds `docs` to that prune step, following the precedent already set for non-site files. The consequence is that the README link to `docs/stylesheet-sync.md` resolves on GitHub, where the README is actually read, and does not resolve on the deployed Pages origin, where nothing links to the README at all. That is the intended trade and it is recorded here so it is not later mistaken for a broken link.

*Licence facts in this section are paraphrased from the linked sources; content was rephrased for compliance with licensing restrictions.*
